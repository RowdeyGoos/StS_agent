# Generic event release v2 contract

2026-09-07. Fresh installable composition of the accepted lifecycle correction.
The user authorized continued correction and live testing; the prior campaign
is closed. Keep the game stopped until this release passes all gates and installs.
Preserve all nineteen frozen predecessors, their contracts and original bridge.

## Exact semantic delta

Derive generic_event_release_v2 from frozen generic_event_release_v1. Source-link
the accepted generic_event_lifecycle_v1 replacements for GenericEventV3Binding,
GenericEventV3CardAdapter, GenericEventV3RemovalAdapter and GenericEventV3RewardAdapter
in production and native fixture Compile closures. Source-link the remaining
frozen v3 core/wire/hooks/parent adapter and card session/rules unchanged. The
button is validated finally at reservation and is an opaque receipt afterward;
all scoped causality, logical context, task, offer, effect and lineage checks remain.
The accepted lifecycle contract and its baseline5/native745/integration73 evidence
own this correction. No new hook, named-event row or capability is added.

All transport, host, security, startup, cleanup and no-retry requirements of the
[frozen v1 release contract](PHASE_1_GENERIC_EVENT_RELEASE_V1_CONTRACT.md) are retained.
Use exact game-owned Harmony, with its existing identity/path/hash/context guard.
No dependency redistribution, extra assembly loader, profile/save/Cloud access,
raw live observations, network dependency, foreign mod changes or remote Git.
The underlying generic_event_v3 wire/config schema and routes remain unchanged.

## New release identities and ownership

New namespace Sts2AgentBridge.Successors.GenericEventReleaseV2. New assembly and
manifest leaf Sts2AgentBridgeGenericEventV2; version1.0.0, two-entry stored ZIP
Sts2AgentBridgeGenericEventV2-1.0.0.zip with the existing canonical manifest shape.
New fixed artifact root /private/tmp/sts-generic-event-v2-release.
Campaign GENERIC-EVENT-V2-SMOKE-V1. State root
Sts2AgentBridgeCampaign-generic-event-v2-smoke-v1. Operator leaf generic_event_v2.
Overlay Sts2AgentBridgeGenericEventV2. Reject the prior generic-v1 state and overlay
as well as every predecessor conflict already rejected by v1. Never reuse old
state hashes, credentials or installed objects; no adoption or overwrite.
The fixed client CLI remains only --expected-state-sha256. Verify all nineteen
predecessor identities and the new frozen source/contract before credential use.

Root owns cloning, exact derivation, client/transport/runtime/package/checker and
docs. Native reviewer owns review of startup/operator/factory/native/production
and operational conflict updates; independent verifier owner owns verifier,
verifier_tests and policy. One SDK lane, explicit handoff, disposable offline
snapshots. Freeze only after complete candidate validation and independent review.

## Acceptance and live sequence

Retain all v1 release fixture categories, exact production metadata/IL verification
and verifier mutations. Verify the four corrected native sources are present and
their old versions excluded from the production closure. Include the accepted
lifecycle baseline/native/integration gate as the correction evidence; execute its
745 assertion native suite in the new aggregate with pinned game Harmony. Retain
actual wire/socket/Python scenarios and correct cleanup retention tests. Build
two identical production candidates, regenerate exact policy/metadata/source
projection and package pins, independently verify, freeze identity and repeat.
Production/sts2/Godot assemblies remain compile-only until installed launch.

Publish only the verified new package. Fresh stopped/closed and exact base checks
precede installation. Verify overlay, protected metadata and stopped/closed state
after installation, then tell the user to launch manually. Profile3,single-player,
fresh Room Full of Cheese initial choices,Gorge untouched,no selector/console/map/
popup. An already-open chooser from a previous session cannot be adopted.
Verify the visible initial state before exactly one new controller invocation.
Uncertain/unsupported outcomes stop without retry and remain unverified.
Expected success is two selected originals added then explicit Proceed/map.
Normal quit, stopped/closed, code-first quarantine, exact purge and clean base
verification close the campaign. Repeated unmodded launch remains waived.

A live success is required before claiming the observed failure is fixed in-game.
If the new attempt stops, retain bounded summary only and diagnose the remaining
predicate in a separately reviewed increment; do not relax ownership to proceed.
