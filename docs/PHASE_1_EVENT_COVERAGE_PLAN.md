# Event coverage development plan

Selected 2026-09-06; priority clarified after implementation commit `b2ae0dd`.
The original baseline was `7e57bb6`; the two completed increments below are
preserved. The generic discovery increment below brings the total to fifteen
frozen successor trees plus the original bridge.
No campaign was left active; this plan update performed no new live check.

## Current direction

The user wants shared handlers that support standard interactions without a
new event-name connection for each event. `generic_event_v1` implements the first
native family: standard upgrade-one through shared request/screen creation and
exact asynchronous ownership, composed through the production native adapter,
card session, wire and Python provider. The
[generic ledger](research/PHASE_1_GENERIC_EVENT_V1_ACCEPTANCE.md) records its
independent review, inert tests, frozen identity and reproducible compilation.
It has no installable release or live validation.

Extend [generic interaction discovery and admission](PHASE_1_GENERIC_EVENT_HANDLER_PLAN.md)
to additional count/operation families. Events primarily supply representative
and held-out tests; custom interactions can need dedicated adapters. The completed
exact rows below remain evidence for their own predecessor components.

## Objective and coverage accounting

Support the pinned build's events through reusable interaction handlers driven
by authoritative interaction descriptions. "All events" is the program objective,
not current evidence. Establish a finite event inventory and record each event's
branches and interaction families with separate implemented, fixture-tested and
live-demonstrated states. An unclassified event or branch is an open gap; the
existence of a shared selector class does not establish its caller's rules.
Natural encounter discovery and strategic option selection are separate from
successfully controlling a user-prepared event.

The predecessor generic event controller handles bounded ordinary choices, one
item child and final Proceed. The predecessor card parent handles only Cheese and Smith.
Its shared pure card core supports add/remove/transform/upgrade with explicit
counts, but this does not enable those operations on arbitrary game events.

The [68-type coverage matrix](research/PHASE_1_EVENT_COVERAGE_MATRIX.md) now records
the metadata inventory and per-type branch gaps. The corrected census established
the type denominator without member bodies or runtime eligibility claims.

## First integrated increment — functional gate passed

The implementation described below is reviewed and its 40-file source identity
is frozen. Candidate and frozen aggregate gates passed, with 244 reported checks
across 12 suites and four matching native builds across the two gates. Repository
regression passed 1,243 tests. The [acceptance ledger](research/PHASE_1_EVENT_ORCHESTRATOR_V1_ACCEPTANCE.md)
records exact evidence. Release composition and live validation remain separate.

Create `event_orchestrator_v1` beside the frozen components. Unify ordinary event
choices, the existing exact item-reward handler and the proven Cheese add-two
card handler beneath one event parent. Add explicit child kind, child ordinal
and accepted-parent-action lineage. Allow a bounded sequence of children with
only one active at a time. The event parent retains sole authority to choose
options and Proceed; the existing card parent must not independently repeat
those actions. Reuse the actual frozen item and card selection implementations.

The Python host gains an immutable observation-to-legal-action decision-provider
seam. Use an explicitly selected deterministic first-legal provider for control tests;
provider output must be validated before one reserved action. This is not a
learned or strategic event policy. Unknown transitions stop with an explicit
unsupported result; no screen adoption after an uncertain action or fallback
clicking is allowed.

A functional gate must cover actual C# core-to-Python composition and compiled
native connections, not only a synthetic standalone router. A later installable
release requires its own complete composition, reproducibility, verifier,
package, operational and live gates. Functional acceptance alone is never
reported as live readiness.

## Second integrated increment — event card bindings

`event_card_operations_v1` adds exact pre-dispatch operation/count/domain rules,
closed unsupported-card classification and shared card-child reconciliation.
The initial new native paths are Aroma of Chaos/Maintain Control and Sapphire
Seed/Eat, both upgrade-one. Their complete native parent/child/Proceed fixtures
and compile-only game-reference builds pass. The 66-file identity is frozen and the
final aggregate passes 675 checks across 19 suites. The
[event-card ledger](research/PHASE_1_EVENT_CARD_OPERATIONS_V1_ACCEPTANCE.md)
records completed functional acceptance. No installable release or live
campaign is implied.

Retained research includes Brain Leech/Share Knowledge add-one; this is a
candidate generic-handler test case, not the next named implementation task.
Its generated choice count comes from the exact `FromCardChoiceCount` dynamic value, captured
before dispatch; complete generated originals are retained once at admission.
Zen Weaver removal requires the remaining exact argument-to-field bindings;
its outer callbacks now have a proven awaited completion witness. Reuse the
shared native selector strategies, retaining card references through cleared
preview highlights. Transformations need authoritative original/replacement
mapping, and multi-upgrade preview originals remain unresolved. These gaps
stay separate from ordinary Smith's proven exactly-one upgrade.

## Coverage expansion

Follow the [generic plan](PHASE_1_GENERIC_EVENT_HANDLER_PLAN.md) for the active
sequence and acceptance criteria. First identify which facts the current catalog
supplies and which can be obtained from an authoritative shared interaction
boundary. Do not infer rules from names, labels, screen shape or observed effects.

Design and review a new contract before changing the pre-dispatch binding rule.
Then exercise actual native/core/wire/host flow for multiple event identities
without adding named rows, and extend by supported interaction family. Keep
unsupported optional/scrolling/custom/combat/repeated-choice behavior explicit.
Separate generic mechanism evidence, event-branch tests, release and live results.
Use bounded retained/static evidence and independent reviews; do not reconstruct
discarded live output or silently broaden observation capabilities.

## Preserved boundaries

Keep public structured observations separate from encoded learning observations.
Keep one owner-frame mutation at a time, recapture before dispatch, reserve
before mutation, exact action/result correlation, bounded total work and no retry
on uncertainty. Parent options remain unavailable while a child is active.
Child completion and exact effect are prerequisites for parent continuation;
ordinary option-screen transitions are not independently verified HP/gold effects.
Every event exit requires explicit Proceed and the bound map handoff.

No profile/save filesystem, Steam Cloud changes, retained live corpus, remote
Git or unrelated capability expansion. The user's event-development request
selects event capability expansion only. Existing frozen contracts/packages and
user changes are preserved; newer contracts supersede scope only in new siblings.
No live game setup is needed during this repository phase.
