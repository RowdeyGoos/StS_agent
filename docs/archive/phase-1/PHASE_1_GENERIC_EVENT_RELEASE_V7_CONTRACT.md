# Generic event release v7: frozen G7 live composition

2026-09-08. The user requested live testing after G7 acceptance at commit
55939fa. Prepare and review a new installable composition before asking the user
to start the game. All28 predecessor successors and the original bridge stay
frozen. This contract changes release composition, never the G7 gameplay engine.

## Shared contract

Source-link the complete accepted generic_event_v7 native, core and wire closure,
including its actual frozen card selection, transform and item dependencies and
the exact generic_event_release_v5 diagnostic enum and codec. No copied or edited gameplay
sources, new native inspection, event-name semantic registration or fallback.
Retain18 exact hooks, retryable owner-thread cleanup, native task/identity/effect
witnesses and finite G7 limits. Native/game production remains compile-only during
offline gates; only pinned game-owned Harmony runs against inert target stubs.

Derive the protected operator/configuration, bootstrap, owner-frame queue,
loopback transport, client, packaging and transactional operations from releasev6.
Use namespace Sts2AgentBridge.Successors.GenericEventReleaseV7, assembly/manifest/
overlay Sts2AgentBridgeGenericEventV7 version1.0.0, stored two-entry archive
Sts2AgentBridgeGenericEventV7-1.0.0.zip, fixed artifact root
/private/tmp/sts-generic-event-v7-release, campaign GENERIC-EVENT-V7-SMOKE-V1,
state Sts2AgentBridgeCampaign-generic-event-v7-smoke-v1, operator leaf
generic_event_v7. Add releasev6 state/overlay conflicts to all previous conflicts.
No historical state, package identity or credential reuse.

The runtime constructs actual GenericEventV7Session and GenericEventV7WireService
around PinnedGenericEventV7NativeAdapter. Native construction remains behind the
existing exact pinned build/Harmony guard and NoInlining boundary. Retain failed
constructor hook recovery and owner-frame service disposal after transport stop.
Keep all78 diagnostic values and mappings; do not invent finer item diagnostics.
The frozen enum and codec remain in their releasev5 namespace; releasev7 consumers
import them explicitly, without changing or duplicating them. Diagnostics describe
the last parent capture and do not claim a precise item/transform failure reason.

Use only /probe/generic-event-v7/public/decision and /action routes. Preserve G7
ordered typed child descriptors and payloads unchanged. Validate card_selection
and item discriminator-specific shapes and canonical action grammar at both
transport boundaries. Items use collect:0..255, cards retain select:0..63,preview,
confirm. Retain the512-byte canonical JSON request cap: the longest item request is248bytes
(collect:255,two64-character decision IDs,ordinal4,choose:7). This is separate from the
unchanged1024-byte HTTP request-head cap (longest valid head481bytes including authentication). Child request correlation remains exactly
ordinal,parent_decision_id,parent_action_id, without kind/version fields. Transport
checks the bounded union action grammar; G7 wire checks compatibility with the
active child. Response classification uses exact descriptor kind+contract_version.
Item resolved and card_transform_v2 resolved responses are nonterminal: continue
until parent map_handoff or actual failure, preserving mixed flows and counters.
Preserve authentication, loopback endpoint127.0.0.1:43117, byte ownership/zeroing,
finite queue/rate/time limits and no retry after uncertain POST. G7 host retains
30seconds,2048reads,12parentactions,4children and52totalattempts. Surface cumulative
completed_card_children and completed_item_children in fixed sanitized summary;
effects remains last-parent outcome, including item_effect_verified.

The first bounded campaign is Aroma of Chaos, upper Let Go (index0 of2).
The exact six-caller metadata capture and retained option/constructor evidence in
research/PHASE_1_GENERIC_EVENT_V7_TEST_CANDIDATE_ACCEPTANCE.md establish a fixed1
request, default transformation factory, awaited TransformToRandom and
SetEventFinished. No preselector HP/gold cost occurs in that callback. Require a
fresh dedicated Profile3 single-player event at its initial two options, more
than1 eligible visible transformable card, no scrolling or open popup/console.
Use an ordinary small starter deck and no added relic modifiers. Fixed policy
requires exactly two initial options and legal choose:0, selects once, then uses
first_legal for child/later parent decisions. This tests one-card transformation
and continuation, not variable-count live behavior. All six known direct callers
use fixed counts; variable G7 remains offline-only evidence. No uninspected-caller
absence claim. Event names are manual fixture selection, never native admission.

G7 variable transformations use card_transform_v2 with positive min<max<=8,
manual=true and complete domain>max. Both fixed/variable G7 transformations use
V2 and preserve exact root-preview reservation, complete preview membership,
retired root receipt behavior, selected-only replacement journal and child
completion history. Release transport must preserve these legal actions and
reject stale V1 tags. No copied transform V1 source is linked unless required by
an actual frozen G7 dependency; exact G7 project closure is authoritative.

## Ownership and gates

Root owns this contract, component scaffolding, production project/checker,
client/package/operations and derivation, release artifacts and living docs.
A owns runtime and transport (except frozen enum), runtime/transport tests and
socket integration projects/fixtures. B owns release native bootstrap/factory,
operator/lifecycle and their inert fixtures; no gameplay replacements. R reviews
contract independently, then owns verifier, verifier tests and exact production
policy. Review remains independent across these boundaries. One SDK build lane;
all builds use disposable snapshots. R reviews root source/operations closure,
root reviews R policy and A/B changes. No live tasks delegated.

Require exact explicit project closure and source hashes, all28 frozen source
inventories, exact derivation/reuse records, two byte-identical production builds,
metadata/IL/source policy and verifier mutations. Preserve meaningful old
operator/bootstrap/runtime/transport/client/package/transactional tests adapted
to the new composition. Old native diagnostic trace suites remain preserved in
releasev5 and are not misreported as G7 execution. Run G7 native/wire/item/core/
host and pure/actual-native integration regressions against the exact linked
sources. Add actual G7 hooks→session→wire→owner-frame socket→Python tests for
variable minimum/intermediate/maximum transforms, explicit root preview, delayed
partial preview, stale V1 envelopes, exact replacement effects and lost preview
or confirmation response without retry. Preserve earlier item, fixed-transform,
multi-upgrade, reward and mixed-lineage socket scenarios. Exercise a fixed1
Aroma-shaped inert flow through the live choose:0 policy plus later Proceed.
Include an offline policy selecting preview below maximum, independent of the
fixed-count live policy, to establish complete release composition coverage.
No controller fake substitutes for native composition evidence.

Freeze source identity after candidate acceptance, rerun the complete release
gate with frozen-client source validation, independently review exact resulting
identities, and only then publish/install. The checker must fail on uncompiled
new sources, stale linked dependencies, altered closure/policy, or package mismatch.

## Live boundary

The user's request authorizes necessary reversible package preparation and the
bounded live test after these gates. Fresh stopped-game/closed-port and exact
429-file clean-base checks precede installation. Verify protected metadata without
credential-content reads plus the exact owned overlay. Then provide exact manual
launch/setup instructions and wait for user readiness. No automatic game launch,
profile/save/Cloud access, foreign-mod mutation, arbitrary commands, broad target
inspection or network dependency. Verify current UI and require-running before
one new client invocation. Never retry or adopt uncertain state, or manually
finish a child selection after an uncertain dispatch. Record only bounded fixed
summary/diagnostic and honestly distinguish card/item completion from latest
effects. Normal quit,stopped/closed,code-first quarantine,exact owned purge and
clean-base/stopped/closed checks close the campaign. Repeated unmodded launch stays
waived under the preserved operational boundary.
