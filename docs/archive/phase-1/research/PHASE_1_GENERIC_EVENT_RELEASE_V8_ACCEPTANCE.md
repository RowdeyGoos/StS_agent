# Generic event release v8 acceptance

2026-09-08. User requested a live off-screen selection test after the
[retained-code verification](PHASE_1_CARD_SELECTION_OFFSCREEN_VERIFICATION.md).
The [contract](../PHASE_1_GENERIC_EVENT_RELEASE_V8_CONTRACT.md) defines a separate
test-only successor30 with G7 protocol and exactly one gameplay replacement.
All29 frozen successor inventories verified unchanged from baseline c6da627.

The probe masks transform action eligibility to fully below-clip candidates and
rechecks the same proof before native input. It never guesses from slot index,
uses mouse coordinates, scrolls or substitutes a different on-screen card.
Success requires the exact selected original in preview and the transformation
journal, followed by event continuation. Production/public Godot execution remains
reserved for the one live invocation; offline fixtures are inert.

## Initial checks

Independent contract review accepted SHA256
`ae6af92e4de9d6c676ce65455baca2572a03137fa5a0b8b27c7d2803f059f7d3`.
Review required rejection of top-level canvas items and non-render parent
separators, bounded parent traversal, and fresh slot-specific proof at dispatch.

Protected client fixtures:13 passed. Prior-state/overlay/operator conflict
fixtures:62 passed. Initial operator/bootstrap focused checks:4 and13 passed,
with clean Release builds; evidence `/private/tmp/generic-release-v8-root-bootstrap-a/result.json`.
These checks do not establish live off-screen selection.

No test build has been installed or live input sent at this checkpoint.
All previous campaigns remain closed. Final identities, aggregate gates and
installation/readiness disposition follow below when completed.

## Focused native, controller and package checks

B's actual-adapter inert suite passed89 assertions, including three event
identities, only slot2 eligible in a20-card domain with mixed clipping, exact
original replacement/map completion, stale geometry and deferred changes.
Pinned production compilation passed without warnings/errors; no production
execution. Evidence `/private/tmp/generic-release-v8-b-d/result.json`.

A's runtime passed855 assertions, transport22 tests and actual native-to-socket-
to-Python50 scenarios (42 retained plus8 off-screen cases). All14 owned sources
match `/private/tmp/generic-release-v8-a-c/source` and its `focused-summary.json`.
The new cases distinguish submitted POSTs from actual native dispatches and
include lost response without retry. Inert setup was corrected to skip an
upgrade-only fixture; the modeled deferred failure is not a live-game result.

Two fresh production compilations match exactly:386048 bytes, SHA256
`3bea54724f61a9d6afe3b89783a6393b1cb5e24adcc68d7129196a7bd3309a68`.
Evidence `/private/tmp/generic-release-v8-production-candidate-a/result.json`
records the56 exact production source hashes. Only the transform adapter is
substituted in the frozen G7 gameplay closure; runtime changes are release identity.

Canonical package checks pass5 tests/9 mutation cases. Manifest352 bytes SHA256
`55636a2930ca9888229ecc7c7c4c8d57c11825a05c29bb4e28023a6c0f725568`;
ZIP386828 bytes SHA256
`5d50e7731ad7b8ace9259712fd2abb604ad6aa6657d9ccc01995d808112d1fd4`.
Transactional lifecycle/fault fixtures pass41 checks after updating the expected
V8 configuration hash to
`745880a427ea75faadd603b6f554c07ddab0eb2d4fe26b09fd060629271156bf`.
No installation or credential generation for a live campaign occurred in these
checks. Final verifier, aggregate gates and source freeze remain pending here.

## Complete gates and source freeze

The first aggregate attempt reached the inert local socket boundary and stopped
with sandbox PermissionError(errno1) on listener.bind; this was an execution
permission issue, not a native/gameplay failure. No source change followed.
The approved local-socket rerun passed every gate:
`/private/tmp/generic-release-v8-root-candidate-b/result.json`, SHA256
`268fd0064b1c67933125dcc41a91e7166f77a0176e40d0df773f2857a70a7fbe`.

Source is frozen as successor30:71 files plus source_identity.json,
inventory `860b8b1062a4668e4ae5df70399a88e2c770606794148a933149b0316700e377`,
manifest SHA256 `3854ed2a09d664a29bed8b079e831cb2db96d2cc5e7aee79e400d201b310a52b`.
Provenance schema2 distinguishes66 derived,3 independently created and51 reused
sources; all six omission/duplication mutations fail as expected. All29
predecessor inventories and the original bridge remain unchanged.

The complete source-frozen aggregate also passes:
`/private/tmp/generic-release-v8-root-frozen-a/result.json`, SHA256
`4d690d0610dfa6b23f32c3ddd9ca60d078938a3986734841679b35d95449f69c`.
It adds successful frozen-client source validation before any credential access.
Both gates pass89 probe assertions,855 runtime assertions,156 verifier checks,
50 socket scenarios,22 transport tests,13 client tests,41 native-release checks,
4 operator/13 bootstrap checks,41 transaction/62 conflict/17 runtime/7 clean-install
fixtures and5 package tests with9 mutations. The verifier binds2150 method bodies
and18 geometry API callsites restricted to the reviewed helpers.

Frozen G7 regressions run separately in each aggregate:3815 native,1718 item,
2650 transform assertions,171 wire cases,118 host tests and201 integrations,
including139 actual-native-with-inert-target cases. These are predecessor
regressions, not execution of the probe's restricted selection eligibility.
The initial pair and both successful aggregate pairs produce six identical
386048-byte production DLLs. No game/Godot/production assembly was executed.

Pre-install checks found the game stopped (3 process samples), port closed
(2 samples), and the unchanged429-file base with0 overlays, projection
`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
Final independent acceptance and installation disposition follow next.

## Final acceptance and installed readiness

R independently rehashed the frozen result/manifest, every current source against
both candidate and both frozen snapshots, all29 predecessors and all six matching
production DLLs. Final acceptance found no remaining publication/install blocker.
Root published the three canonical artifacts, then installed exactly once through
the frozen transactional manager. Campaign GENERIC-EVENT-V8-SMOKE-V1 is installed;
mods_parent_created=true. Fresh current installed state SHA256:
`c5624818b227412d0290018eb05462e0b5a8780c75d3c9b9b54bae2fc5a25c05`.

Overlay verification passes:429 unchanged base files and2 owned overlay files,
projection `d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
Frozen client source validation and validate_installed_for_client pass; the latter
verified protected metadata without reading credential contents. Results are
`/private/tmp/generic-release-v8-install-result.json` and
`/private/tmp/generic-release-v8-overlay-verification.json`.

Preparation is complete. No game launch or live client invocation has occurred.
Await user readiness after manual launch on Profile3, single-player, same window
size/resolution as the last five-column test, an ordinary20-card deck and a fresh
Aroma of Chaos event at its initial Let Go/Maintain Control options. Leave both
untouched, close console/popups, and do not scroll or select cards during the test.
Do not resume/adopt the earlier open selector. The bridge chooses Let Go and an
actually proved fully clipped allocated card, confirms the exact transformation,
and proceeds. Twenty cards targets a fourth row; runtime proof, not deck count
or screenshot inference, determines eligibility.

Once ready, verify current UI and require-running, then invoke this frozen
release's client/run_live.py exactly once with the current installed-state hash.
No retries after uncertain input. Normal quit, wait-stopped, code-first quarantine,
exact four-file purge and final clean-base/stopped checks remain owned cleanup
for this installed campaign after its live invocation. Earlier state hashes and
closed-campaign results are historical. All30 successors remain frozen; live
success is still pending.

## Single live invocation and completed cleanup

2026-09-08. This closes the installed-readiness checkpoint above. User said ready;
CUA screenshot showed initial Aroma of Chaos options, deck20, no console/popups.
Profile identity was operator setup, not independently established by this event
screenshot. Fresh require-running passed (one process sample). The frozen client
was invoked exactly once with installed state
`c5624818b227412d0290018eb05462e0b5a8780c75d3c9b9b54bae2fc5a25c05`.
It exited4; sanitized result:

```json
{"schema_version":1,"status":"failed","parent_attempted":1,"parent_accepted":1,"parent_reconciled":0,"child_episodes":0,"child_attempted":0,"child_accepted":0,"child_reconciled":0,"total_attempted":1,"reads":258,"effects":"unverified","completed_card_children":0,"completed_item_children":0,"code":"unsupported_state","last_response_diagnostic":"prepare_geometry"}
```

Result file `/private/tmp/generic-release-v8-live-result.json`, SHA256
`9b8780fc41cc828f7f8e6f280286109ba6136cdcfd12902a594aa5534b1b55eb`.

Post-test screenshot showed the open transform selector, deck20, five columns,
two fully visible rows and a clipped third row; no transformation preview.
The accepted parent action opened the selector. Zero child episodes/actions
means the probe did not reach card selection. Effects remain unverified as
reported; no selected-original replacement or event continuation is claimed.
No retry, manual card completion or additional bridge invocation occurred.

Static localization: TryPrepareSurface assigns PrepareGeometry after holder
snapshot and candidate binding succeed. That label covers GridGeometry.TryBind
(scroll/layout validation plus ProbeGeometry binding), geometry.Mask and
InitialCandidatesValid. The latter also rejects selected/invisible candidates
or a mask with no enabled candidate. The result does not distinguish these
predicates and contains no numeric runtime geometry. Passing earlier binding
stages is not a retained full runtime inventory. Neither screenshot nor this
result proves native off-screen selection impossible. Removing the fit inequality
was insufficient for this test to reach dispatch; the exact remaining rejection
must be diagnosed instead of inferred from deck count.

Normal UI quit returned App quit; wait-stopped passed with three process samples
and two closed-port samples. Code-first quarantine passed, returning fresh state
`bcd301217b6df0c4025b5a89525b125f53a8134635c27b022dd58c7af67a86e4`.
Purge used that hash and passed: phase absent, generated_files_removed4.
Final require-stopped passed (three process/two port samples), and base verification
passed:429 files,0 overlays, projection
`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
No profile/save/Cloud access was used. Cleanup is complete; all campaign state
hashes above are historical and must not be reused.

Disposition: offline gates remain accepted; live off-screen selection remains
unproved. All30 successors stay frozen. Before another live test, prepare a
separate successor that reports bounded per-predicate geometry failures (without
raw observations or credentials), with matching contract and offline validation.
No new campaign is installed and no further user game setup is needed now.
