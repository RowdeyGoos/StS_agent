# Generic event v1 acceptance

2026-09-06. Active integration checkout `23cf`, branch
`codex/phase1-actor-ready-integration`, baseline `6a5ae69`.
User request: read Astra handoff and implement the generic event handler.

## Architecture and ownership

The [contract](../PHASE_1_GENERIC_EVENT_V1_CONTRACT.md) replaces predeclared
named-policy admission only in a new sibling. Read-only audit verified retained
selector and EventOption metadata/IL hashes. No new static target inspection was
needed. The independently reviewed three-call observational boundary captures
operation from the upgrade command, preferences/domain from actual screen
creation and completion from the exact request/selector/Chosen tasks.

Root owns contract, wire/checker/documentation and integration acceptance. Native
lane owns core/native and native fixtures. Host lane owns strict host fixtures and
independent source review. Integration lane owns protocol and cross-language
fixtures. All predecessor source remains unchanged. New Harmony 2.4.2 is confined
to this successor; package and DLL hashes are in its dependency manifest.
Real inert prefix/unpatch smoke passed before implementation; no target ran.

Review corrections include exact run-state and whole-deck admission, both native
manual preference values, returned request-task agreement, buffering incomplete
creation, dispatch causality, task/screen tombstones and truthful post-dispatch
failure accounting. New interfaces use no event-name catalog.

## Candidate verification

Initial native fixtures passed 111 assertions, wire 29 cases and host 31 cases.
The initial cross-language suite passed 16 cases. These were intermediate results.

Root aggregate attempt A verified all fourteen frozen predecessors and original
48 bridge files. Compile-only target binding then found two ambiguous Environment
references (System versus Godot). These were qualified as System.Environment;
attempt A was not accepted and executed no target assembly.

Final candidate B passed in `/private/tmp/generic-event-root-check-b/result.json`:

- 200 native assertions using real observational hooks on inert target stubs.
- 34 wire checks and 33 Python host tests.
- 20 integration checks, including four through the production native hooks,
  native card adapter, actual parent/card sessions, wire and Python provider.
  Three unrelated event subclasses include a held-out identity; the fourth
  production-native case exercises delayed discovery.
- All fourteen predecessor source identities and original 48 bridge files intact.
- Two compile-only native builds byte-identical: 39,936 bytes, SHA-256
  `31c0bc5dea50b832eec96bace28a77d301c1ebd417ae2b062962f1576780f971`.
- Seven explicit project closures checked; required test inputs and exact derived
  source diffs verified. No target assemblies executed or copied into outputs.

Final independent review accepted the code, partial hook-installation recovery,
source derivations and checker with no remaining actionable findings. Candidate
B's reviewed inventory was
`68ef8e0815f878edffe1c9c1e54216cd9bc9f1bd039907bc5a9d998caa0ef37f`.
Only the component README changed afterward, documenting the real synchronizer
lifecycle residual below; executable source remained identical.

## Frozen functional acceptance

The final frozen aggregate passed in
`/private/tmp/generic-event-root-frozen-a/result.json`; its exact result is retained
[in the repository](PHASE_1_GENERIC_EVENT_V1_FROZEN_RESULT.json).
Result SHA-256: `74416a2cb0f70f03d6d138b1390ed63a23ee46ec388503365b5ed47557d0c836`.

The 30-file component manifest is frozen:

- Manifest SHA-256:
  `93bb01fcbad636647994e2ad3459aec260a98d971e1f4be102157273b9f6a2f4`.
- Source inventory SHA-256:
  `1771c317901b99447ed5314c054e6d4abccde597652da28d322ff49211958f36`.
- Contract SHA-256:
  `8cc1c1c9fd4ae6d8e4a12fab5dccd33f4c8ffb79648c28eada833dc1c1b7e9f4`.

The frozen gate repeated all 200 native, 34 wire, 33 host and 20 integration
checks successfully, including four production-native integrations. It verified
the new source identity and all fourteen predecessors. Both native builds matched
each other and candidate B exactly: four matching builds across the two gates.
There are now fifteen frozen successor trees plus the original bridge boundary.
Functional acceptance is complete for the stated single-upgrade family; release
and live acceptance remain open.

Repository regression: 1,300 passed and one existing sandbox-only localhost bind
failure. The unchanged inert socket fixture passed separately with local
permission. Thus all 1,301 collected repository tests passed across two
invocations; this is not one clean full-run result. `compileall game tests` passed.
The staged whitespace check reports one whitespace-only line at
`core/GenericEventV1Session.cs:221`; the accepted source identity is preserved
rather than changed for this cosmetic blank line. All other staged files pass.

## Evidence boundary

Generic native support currently covers noncancelable upgrade-one with2..64
complete candidates and native preview confirmation, including identities absent
from the production catalogs. There is no all-event or all-branch coverage claim.
Add/remove/transform, multi-upgrade, optional/scrolling, custom/combat and repeated
stable-key families remain explicit gaps. No new release, listener, package,
installation, profile/save access, live campaign or cleanup operation occurred.

Ordinary target option dispatch passes through EventSynchronizer before Chosen.
The retained static API audit establishes this route; compile-only and inert
fixtures cannot establish whether the actual game preserves ExecutionContext
through it. The implemented binding stops if scoped causality is lost. Real
synchronizer lifecycle and hook compatibility remain release/live acceptance
gates, not demonstrated properties of the current fixture result.
