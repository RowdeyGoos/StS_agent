# Generic event release v8: off-screen selection experiment

2026-09-08. User requests a live test of the retained-code finding in
research/PHASE_1_CARD_SELECTION_OFFSCREEN_VERIFICATION.md. Successor30 is a
separate test composition; all29 predecessors and original bridge remain frozen.
This is not general scrolling support or a new general transformation release.

## Exact experimental change

Derive releasev7 protected operator/bootstrap/transport/client/packaging/operations
under GenericEventReleaseV8, assembly/overlay Sts2AgentBridgeGenericEventV8,
version1.0.0, stored two-entry Sts2AgentBridgeGenericEventV8-1.0.0.zip,
artifact root /private/tmp/sts-generic-event-v8-release, operator generic_event_v8,
config generic_event_v8_transport_config_v1, campaign GENERIC-EVENT-V8-SMOKE-V1,
state Sts2AgentBridgeCampaign-generic-event-v8-smoke-v1. Reject all earlier
state/overlay/operator conflicts. No historical identity or credential reuse.

Retain G7 core, protocol, routes and host, transform V2 journal,18 observational
hooks and every gameplay source except GenericEventV7TransformAdapter.cs.
Derive that file into gameplay/ with its existing type/namespace so frozen
callers link exactly one replacement. Explicit project closure must exclude the
old adapter and include this one. Keep all other compiled gameplay bytes exact.
No reflection/private invocation, direct signal emission, model mutation, input
fallback, mouse coordinates or scrolling. Dispatch remains holder._GuiInput
with the existing select InputEventAction on the owner frame.

Remove only the complete-layout-fits inequality from transform geometry
admission. Preserve finite positive sizes, exact scroll-size/position formula,
complete expected original coverage by unique holders, stable geometry and all
holder/card/hitbox/highlight/material/model identities and task/effect ownership.
This may still reject a scrolling grid whose allocated holder set is incomplete.

This test adapter intentionally permits transform selection only for candidates
proved fully below a real clipping region. Preserve native tree visibility and
selected flags; mask only public action eligibility. No qualifying candidate
means no input, never fallback to an on-screen candidate. Every select delegate
must independently revalidate the same proof immediately before _GuiInput.
The existing first_legal policy therefore selects a proved candidate rather than
guessing its slot. No new protocol field or global witness state is needed.

## Runtime geometry proof

Walk at most32 parent links from the bound grid to the nearest live Control
with ClipContents=true. Retain the exact clip and parent identities; require
holder/card/hitbox ancestry through that clip. Reject top-level CanvasItems in
the candidate-to-clip ancestry via IsSetAsTopLevel; clipping must be inherited.
Use public GetCanvas RID equality,
GetGlobalTransform and Control.GetGlobalRect in the same canvas coordinate space.
Relevant transforms must be finite, axis-aligned and positively scaled, with zero
off-diagonal basis entries. Rectangles must be finite and strictly positive;
holder, card and hitbox rectangle tops must each be strictly greater than the
clip rectangle bottom. Partial overlap, touching boundary, invalid/zero geometry,
rotation/shear/reflection, foreign canvas, replaced clip or changed parent chain
reject selection. Capture and revalidate exact relevant geometry and identities
before dispatch, not just during a prior GET. Keep selected-only preview and final
journal validation; deferred holder reassignment must never select a different
original successfully. Unexpected changes stop without retry.

New runtime reads are bounded to these already bound controls and their <=32
parent chains. No new target assembly inspection is needed; compile against
pinned cached references and validate public getter usage in release metadata/IL
policy. Offline tests use inert Godot/game objects. No execution of production,
sts2.dll or GodotSharp.dll during offline gates. Runtime assumptions are tested
live, not claimed from inert stubs. The result means below the validated selector
clipping region, not necessarily outside the entire game window.

## Ownership and validation

Root owns contract, scaffolding, project files, checker, provenance, client,
operator/bootstrap derivations, package/operations, artifacts and living docs.
B owns gameplay/GenericEventV7TransformAdapter.cs, gameplay_tests/TargetStubs.cs
and gameplay_tests/OffscreenFixtures.cs. A owns runtime/transport and their tests,
integration sources/Python tests, coordinating inert fixture geometry with B.
R independently reviews contract and implementation, owns verifier/verifier_tests
and policy, and reviews root operations/source closure. No frozen writes or live
work delegated. SDK9.0.303 builds serialize through root in /private/tmp only.

Run frozen G7 full regressions separately and report them as predecessor evidence.
Test the actual replacement adapter through native and socket-to-Python paths:
full-below selection to exact original preview, final selected-only transformation,
Proceed and retained child completion. Cover incomplete allocation, no clipping
control, partial/touching geometry, invalid transform/canvas, changed clip/parent/
rectangles, recycled holder/card, deferred reassignment and uncertain response
without retry. Preserve relevant bootstrap/operator/runtime/transport/client and
transactional cleanup tests. Do not report old on-screen transform fixtures as
successful execution of this intentionally restricted probe unless their setup
is explicitly updated and independently reviewed.

Require exact source closure/hashes and provenance, all29 frozen inventories,
public API and source/IL verifier with negative mutations, two byte-identical
production compilations, candidate acceptance then full source-frozen aggregate
acceptance and independent review before installation. No general off-screen
support claim follows from preparing this probe.

## Live test and cleanup

The user's request authorizes necessary reversible preparation, exact owned
installation and one bounded live test after these gates. Require fresh game
stopped/ports closed and exact429-file clean base; install only the owned overlay
and private operator files using derived transactional tooling. Verify installation
without exposing credentials. Then tell the user to launch manually on dedicated
Profile3, single-player, Aroma of Chaos initial two choices, both untouched,
console/popups closed. Use an ordinary deck large enough to have a fully clipped
allocated row (target20 cards at the previously observed5-column layout), without
added relic modifiers. Do not promise exact live allocation/clip properties from
the earlier screenshot. Do not use a ten-card fit-only setup.

Fresh UI and require-running checks precede exactly one new client invocation.
Fixed parent policy chooses Let Go index0 of2; first_legal uses native offscreen
eligibility for the child, confirms its exact preview and proceeds. If no eligible
candidate, geometry mismatch, task fault or uncertain input occurs, stop without
retry/adopting an open selector or manually completing it. Success requires one
verified card child and normal parent continuation, not merely input acceptance.
Record the fixed sanitized summary and visible result without raw model dumps.

No automatic launch, profile/save/Cloud access or foreign-mod changes. Normal
quit, stopped/closed checks, code-first quarantine, exact owned four-file purge
and unchanged clean-base/final stopped/closed checks close the campaign. Live
preparation is not complete until installation is verified; only then ask the
user to start the game. No redundant permission request is needed for this
already requested test.
