# Generic item discovery acceptance

2026-09-08. Metadata-only dependency for the G6 singleton potion/relic family.
This evidence changes no frozen successor and establishes no live support.

The reviewed scope is docs/research/PHASE_1_GENERIC_ITEM_SCOPE.md, SHA256
`d659a93c6a21161dc7fe94e11964a4037440d1a69b190f472b42ac93e273e411`.
It permits exactly seven method bodies and one field declaration on the pinned
9,363,456-byte sts2.dll SHA256
`e7ceb80669bfaf5c8fccabaa126ae2bb283aba514be5b5b55612579cfd285f18`.
Three method caps are2,500 instructions; four passive-getter checks are64 each;
total ceiling7,756. All user strings are redacted and no callee is followed.

Inspector root: `/private/tmp/generic-item-inspector-a`.
The initial wrapper created fixture output before checking nonsymlink/regular
shape. Independent review required moving this guard ahead of output creation.
The first toolset is preserved under `prior-reviewed-a`; it never read the target.
The corrected tool passes58 synthetic checks, including invalid fixture paths
with no output creation, selector/signature/body limits, bounded output, timeout,
redaction and exact schema. No synthetic or target game assembly executes.

Independently rehashed corrected manifests:

- source44: `fc32cc71d3637a1736c8f003ad24a111e14b4d927085bdd40819f09ca3d72675`
- bundle3: `9cd165d9a67f0e1ddae8f49795e4980bb80eb7132f6715d32ac8b954d7459b3d`
- synthetic19: `38923524cf056ed4b2fc995f870f8469b115ac0e83c97d7c719d336dbe345788`
- synthetic result: `90c9b5641e71325bcb4e7019bd6d684b02a7526900118308d58e976310a15fb5`
- inspector DLL43,520 bytes: `e25c09669afc4db894e496e70c29b3e4bcc0e665c9c3c1a2f34a69244504860c`

Root's separately reviewed one-shot command uses the fixed target mode, exact
three-file bundle, clean environment and fresh `/private/tmp/generic-item-target-a`.
Capture bounds are1MiB stdout,4,096bytes stderr and30seconds, with private create-only
files and kill/reap on timeout or overflow. No target override, retry, broad scan,
profile/save/Cloud access, launch or installation is included.

## One-shot result

Root executed the exact reviewed command once. It passed with7bodies,1field and
338instructions. Retained stdout SHA256
`3572bc4a8170ec1d2a60951fae2743cea4d00ea90cde0f0502ac0b8d32953605`;
stderr is empty. The four getters are passive instance field loads. Offer awaits
generation before screen creation and awaits its rewards-set synchronization
task afterwards. ShowScreen stores the exact set/run and terminal Boolean, pushes
the exact newly created screen and returns it. Details and exclusions are in
[the native evidence](PHASE_1_GENERIC_ITEM_NATIVE_EVIDENCE.md).

No target assembly executed, no live game started and no profile/save/Cloud data
was accessed. This resolves a static implementation dependency and does not
claim real EventSynchronizer execution or successful live item handling.

The exact returned summary is retained as private create-only `result.json`,
SHA256 `786bf1bd01ab457a65253b1ceaaaa4a8b14ee8bc509c3ccec5adbffe0520d42a`.
Root verified the capture directory0700 and all capture files0600. Raw stdout
is47,112bytes. Independent result review accepted all selected method/field
attributes, getter passivity and the bounded native implementation interpretation.
The full G6 contract was independently accepted at SHA256
`fa2a970c7a0a446d0993691bf9d1296f9ef846fd40befd0ed1019ab827147552`
before B began production implementation.
