# Generic event release v3 diagnostic acceptance

2026-09-07. Accepted and installed on authoritative23cf, baseline8274e30. The user
authorized continued work after the v2 timeout and completed cleanup. Campaign
GENERIC-EVENT-V3-SMOKE-V1 completed one failed live invocation with a bounded
prepare_candidates diagnostic and full owned cleanup. The final section owns
the current closed state; all installed-state statements are historical.

## Reviewed contract and ownership

The [contract](../PHASE_1_GENERIC_EVENT_RELEASE_V3_CONTRACT.md) was independently
accepted before implementation. SHA256:
`446dc46b1f43ff91834d83e488d1988371183473b477ec86f64002faed6eb4d1`.
It adds finite diagnostic history at existing native checks and an exact HTTP
header, keeping frozen gameplay core, wire, host, hooks and admission semantics.
The final field is explicitly transport-validated history and may retain an older
native reason across a core timeout; it does not certify JSON semantic acceptance
or current outcome cause. Provider failure cannot change valid gameplay outcomes.

Root owns release composition, checker/client/package/operations and docs. B owns
native instrumentation, enum, production factory and native fixtures. A owns
runtime, strict header/Python transport and socket integration. R owns independent
contract/source review and verifier/policy/mutations. One SDK lane, disposable
snapshots, exact game-Harmony identity, no offline target-game execution.

All twenty predecessors and the original bridge remain frozen. New v3 release
identities prevent adoption of either closed generic campaign, with explicit
v1/v2 state/overlay conflicts before install and client credential access.
Candidate validation, independent verification, source freeze, frozen aggregate
and fresh installation remain pending. No live result is claimed.

## Preliminary candidate and root-owned checks

Two compile-only production builds matched at235008bytes,SHA256
`4a3f32c5786e9b291efccbfb7b48bd177796ab12c2378ab873facb74624cb6a9`,
under `/private/tmp/generic-release-v3-production-candidate-a/{first,second}`.
Manifest352bytes,SHA256
`82b0c18c87de98c7aab88d9c09b6faac43d3ad041540dd2c5aa39a0889c36ab5`;
stored ZIP235788bytes,SHA256
`ab6258c1124267a926ff6eb0077dc83f93f3cbc5b00313dec9ab578c98d4f396`.
These are candidate pins; publication remains gated on complete acceptance.

Root-focused checks passed13 client tests,41 manager groups,32 predecessor
conflicts and5 package cases with9 mutations. Independent review corrected a
v2-conflict fixture's expected state-name literal before those tests; both new
state and overlay checks now exercise late client/quarantine retention. Exact
source validation passed all20 frozen predecessors. No credential was read.

The diagnostic fixture derives the frozen lifecycle Program with only an optional
RewardFixture nonce argument, allowing the actual socket runtime-generated nonce.
All745 original assertions remain unchanged; frozen lifecycle TargetStubs are
source-linked. Root reviewed the exact two-line functional fixture delta; retained
and new native tests must pass against the four instrumented replacements.

## Native and transport focused evidence

B's isolated pinned-Harmony/inert-stub suites passed745 preserved assertions
against the instrumented native replacements,168 dedicated diagnostic assertions
covering all36 native codes, and38 native-release assertions (28 retained plus10
once-bound target and failed-unpatch cleanup/retention/clear checks). Artifacts
are under `/private/tmp/generic-release-v3-b-diagnostic-a/artifacts`.
Production sources remain identical to the preliminary candidate pair.

A's Python transport suite passed17 tests. The extended existing socket fixture
source-links actual instrumented parent/child adapters, frozen lifecycle binding
and stubs, and the derived nonce-aware native fixture, preserving the old socket
scenarios. Runtime/socket execution and independent binary verification remain
pending before aggregate acceptance.

A's focused Release runtime suite passed373 assertions; all15 socket scenarios
passed (eight preserved plus provider-failure and five actual-native scenarios).
Actual native pending_screen and prepare_geometry remained attached through the
256-pending-read core budget and final unsupported response. The resumed selector
fixture reported prepare_geometry, then child_ready and map_ready. Invalid or
throwing diagnostic providers preserved successful gameplay with the fixed
diagnostic_unavailable code. Owner-thread sampling, detached result discard,
provider retention/clear and no-retry outcomes passed. These are inert fixture
results and do not identify the predicate responsible for the v2 live timeout.
Root independently compared all38 production Compile inputs with candidate-a;
every byte still matched after focused implementation and test completion.

Independent source review found no actionable issues in the native predicates,
once-bound factory cleanup, runtime/queue response pairing, HTTP/Python validation,
actual-native socket scenarios or root release/client/operational composition.
The review confirmed11 projects and38 production sources, all four instrumented
replacements in production/native/socket closures, exact20-predecessor chaining,
client fallback none and explicit closed-campaign conflicts. Candidate binary
verification and complete candidate/frozen aggregates still gate publication.

## Independent binary acceptance and aggregate attempt

R's verifier passed73 checks (63 retained plus10 diagnostic checks). Both
production candidates independently verified and extracted byte-identical policy:
984317bytes,SHA256
`81b6c6b01554156fe6cdd855c99f6fd63241ad9ba39395bd2ef0e5b2e258c2e1`.
It covers1221 method bodies,8987 inventory rows and38 sources. Source projection
`672d1f00215cc1c70f20c45f4f7101975c16efc004b4f41db9541ba0ec472f2d`;
metadata projection
`83fb730ce950d97dac212c8e42f387d8d66c8144f2e962757bac25f3a3f5590a`.
Independent metadata/IL checks prove the enum values, literal mapping, cached-field
getter and sampling callsites without CLR execution of the candidate. Outputs:
`/private/tmp/generic-release-v3-verifier-a`.

The first full aggregate at `/private/tmp/generic-release-v3-root-candidate-a`
stopped at log037, the runtime-tool inert loopback fixture. Direct inspection of
that same disposable fixture exposed PermissionError at listener.bind on an
ephemeral localhost port, imposed by the sandbox. This was not a gameplay failure.
The unchanged full suite was restarted with scoped local-socket permission in a
fresh `/private/tmp/generic-release-v3-root-candidate-b`. No live request occurred.
Staged whitespace validation passed before source freeze. Exact derivation now
contains66 predecessor-to-successor source/project rows.

## Candidate aggregate and source freeze

The unchanged full candidate aggregate passed at
`/private/tmp/generic-release-v3-root-candidate-b/result.json`. It passed all
categories above plus13 bootstrap groups,4 operator groups,17 runtime-tool groups,
7 clean-install groups and the frozen predecessor745 native assertions. Two
production builds matched the independently reviewed235008-byte candidate.
All20 predecessor identities and all11 project closures passed.

The74-file release source is frozen after independent review, candidate acceptance
and staged whitespace validation. Inventory SHA256:
`ae2467e5f838e2d503bb881a258fa246a200227171653f8137fe2d170a00203d`;
source manifest SHA256:
`e16ddced1379e250ed8c40e8cf0807fad0556850f41738a85cd4b16c872edbfb`.
Client source validation passed without credential access. Final frozen aggregate
is running at `/private/tmp/generic-release-v3-root-frozen-a` before publication.
Do not edit the frozen source tree, contract, identity, derivation or policy.


## Final acceptance and installed campaign

Frozen aggregate passed at `/private/tmp/generic-release-v3-root-frozen-a/result.json`,
SHA256`b4488051ffebcce9a25c3cb716bd11f4b5436c70b5ae58c74de6e2ef7471646d`.
All candidate categories passed again, including frozen client source verification
without credential access and two more matching production builds. The release
is accepted and frozen as successor21; all20 predecessors remain unchanged.

The canonical three-file artifact set was published at
`/private/tmp/sts-generic-event-v3-release`. Fresh stopped/closed preflight passed.
The first base-verifier call rejected a relative manifest argument before reading
it; the corrected absolute-path invocation passed429 base files, zero overlays,
projection`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
Authorized fresh installation then passed for GENERIC-EVENT-V3-SMOKE-V1, generic
flow, mods_parent_created true. Installed state SHA256:
`c3e2d3f1990e9e1c7210d70de0213db03ad44aceb706e5ec2dd44f4bac27de6f`.
Post-install verification passed:429 unchanged base files, exactly2 matching
overlay files, game stopped and port closed, and frozen client/protected metadata
validation without credential access. No game launch or live invocation occurred.

The bridge is installed awaiting manual launch: Profile3, single-player, fresh
Room Full of Cheese initial options, Gorge untouched, no selector/console/map/
popup. Verify visible state before exactly one current-client invocation. Use
only this release and installed state identity; never adopt an old selector or
reuse closed campaign state. Diagnostics are fixture-proven; the actual predicate
behind v2's timeout remains unknown until a fresh live result. No in-game repair
or successful child/effect claim is made by this diagnostic release.

Cleanup remains pending after the fresh campaign: normal quit, stopped/closed,
this release's code-first quarantine and exact purge using current state lineage,
then clean base and stopped/closed checks. Repeated unmodded launch is waived.


## First v3 live invocation and complete cleanup — 2026-09-07

The user reported ready. Supported computer-use observation verified the fresh
Room Full of Cheese initial choices, Gorge untouched, with no selector/console/
map/popup. The running-game check passed. Exactly one current-client invocation
used the installed state identity above and exited4 with the bounded summary:

```json
{"schema_version":1,"status":"failed","parent_attempted":1,"parent_accepted":1,"parent_reconciled":0,"child_episodes":0,"child_attempted":0,"child_accepted":0,"child_reconciled":0,"total_attempted":1,"reads":258,"effects":"unverified","code":"unsupported_state","last_response_diagnostic":"prepare_candidates"}
```

UI observation showed the eight-card chooser with a choose-two prompt and no
selected cards. No live response corpus, card identifiers, raw bridge observations,
retry, second invocation or manual selection was retained or performed.

The last completed native capture reached the reward adapter's TryCreateBindings
stage and did not establish child readiness. Its prior binding/context, exact
overlay, deck/offers, foreground, grid and holder checks reached that stage.
The frozen core eventually stopped at its pending-read budget and retained the
last capture diagnostic. This is the measured v3 blocking stage, not proof of
which nested check failed or what caused the historical v2 stop. Candidate
preparation still includes offered-model reference membership/uniqueness, card/
hitbox/highlight/material identity, stable keys, snapshot coherence, highlight
settling and initial visibility/enabled/selection checks. Geometry is evaluated
later and is not established passing or failing by this result. No child actions
or successful effects are demonstrated.

Normal Cmd-Q closed the game. The wait-stopped check passed (three process samples,
two port samples). Code-first quarantine passed with state SHA256
`ef54e41172876f0e880de945d7694073ca2c3260d2e021c0b664a2d3a92ecd37`.
Exact purge passed with phase absent and four generated files removed. Final
base verification passed429files,zero overlays,projection
`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`;
final require-stopped passed with three process and two port samples. Repeated
unmodded launch remained waived. No profile/save/Cloud work or unrelated changes.

No campaign, credential, overlay or cleanup remains active. Both installed and
quarantine identities in this ledger are historical and must not be reused.
Next inspect/reproduce the candidate-binding and candidate-state subchecks in a
reviewed successor; narrow diagnostics only if retained static evidence and
inert fixtures cannot establish the mismatch. Do not relax identity or ownership
rules based on this stage code, and do not replay the closed campaign. Keep the
game closed until a separately gated fresh release is ready.
