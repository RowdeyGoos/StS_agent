# Event card operations v1 development ledger

Started 2026-09-06 against local commit `920ec84` in the existing 23cf integration
checkout. The user asked to continue development toward all event support.
The thirteen accepted successor components and original48 remain frozen.
No campaign is active. All-event support remains an objective, not current evidence.

## Active dependency graph and ownership

| Packet | Owner | Writable scope | Dependency and acceptance |
| --- | --- | --- | --- |
| Card-caller census preparation | B | New `PHASE_1_EVENT_CARD_CALLER_*` research docs and disposable `/private/tmp/event-card-callers-b` tool | Exact scope, source review and synthetic gate before one target invocation |
| Reusable native adapter design | A | `PHASE_1_EVENT_CARD_OPERATIONS_DESIGN.md` | Repository-only design against frozen core and new caller evidence |
| Contract and integration | Coordinator | New successor contract, this ledger, living status/plan, later explicitly assigned component files | Independent contract review before consumers |
| Independent review | R | Read-only initially | Scope/tool, contract/API, source and final evidence review |

The first dependency is exact event-option callback to selector/effect evidence.
A selector reference alone cannot establish operation, cardinality, complete domain,
confirmation mode or effect completion. Prefer bounded one-shot removal, upgrade
and addition callers. Transformation requires authoritative original-to-replacement
binding; optional selection, repeated stable options and custom/combat interactions
retain separate open gates. Preserve multi-card event requirements and Smith's
existing exactly-one behavior.

The successor should reuse frozen sessions and unchanged source wherever possible,
with checked derivations only for new policy/adapter connections. The existing
orchestrator's closed Cheese policy and wire validation cannot be silently
broadened. New behavior requires its own explicit versioned contract and complete
actual-core/native-fixture/wire/host composition tests before source acceptance.
No component implementation or new static target read is accepted by this initial
planning record. Record subsequent exact review and execution dispositions below.

No profile/save filesystem, Steam Cloud change, retained live corpus, target
assembly execution, remote Git or unrelated capability expansion is selected.
Live release composition and installation remain separate from functional work.

## Structural contract accepted

R independently accepted the row-free structural contract at SHA256
`be480799460c5a5799bd9785d21f1c41d6467c912876cd456b271999408dac6e`.
The coordinator accepts those exact bytes. The retained draft banner is superseded
by this disposition. The executable API still requires independent review before
consumers; A owns the new `core/` API draft. Root owns later wire/host/schema and
aggregate work; R owns independent review and later integration fixtures. B owns
static caller tooling and evidence before later native implementation assignment.

This first stage selects generic validation only. Production caller rows, new
native adapters/factories and supported-card policy allowlists remain absent until
a separate exact evidence-bound caller-selection disposition. The structural
contract will not be mutated to add rows. Frozen predecessors remain unchanged.

## Structural API accepted for generic implementation

R and the coordinator accept `core/EventOrchestratorV1Contracts.cs` at SHA256
`9275a7773f5685e06546300e0e3c8ca4b4651e16d96c75a259c51fde8873b956`.
The draft source comment is superseded without changing those bytes. The separate
catalog implementation currently contains only the accepted Cheese row and is
not part of the frozen API; new rows still require the second-stage disposition.
Its reviewed initial SHA is
`03f2d85efc441a402fc228968e51ca20407710b51d66df1099dc0988a181da29`.
The initial core project SHA is
`b2f450df67e89646ffb8440e9d230b3b201f19af76747d93b4ca35ae58a6fdd7`;
project compile lists remain implementation-owned until the final closure gate.

A now owns generic core session/encoder/catalog, core tests and their project
files. Coordinator owns wire/host/schema, their tests, shared build properties,
aggregate/derivation gates and living docs. R owns independent source review and
later actual integration fixtures. B retains exclusive static caller scope/tool
ownership; native implementation remains blocked on exact caller evidence.

## Wire schema and caller scan activation

R and coordinator accept the row-free wire schema at SHA256
`22c563ac03aff7ef522ba2f56e09fb22ea586a44beebd491381130c3d7884eec`.
Generic wire/host consumers are activated against that exact schema and API.

R reviewed the caller scan and required exact event/API configuration hashes and
68/28 counts before target access. The correction passed 14 inert checks. R and
coordinator accept scope SHA
`b2bd845547c23884b86bfb26f1a7228c66c87508da76ef781b706547b7012677`,
Program SHA `beb1a92c9d65523673a1cfb556d79609d839741ec2a30e318edca49dfc6a55fb`,
runner SHA `c7ada32c3006a60040f9a74a74b47c747c55c37f07af2e14fae89b754a5ddf51`,
21-input manifest SHA
`d8c5a1026dfeabbe5458a8d95b6bf6ac7f72ff96d4db43fb6946cd669370365c`
and synthetic record SHA
`f16bcf1058810bd6ec0e5ee54ccf1bbac2cf4fcf5385af12c2597237d5492caa`.
The coordinator authorizes B to run exactly one invocation against the pinned
`sts2.dll` under that finite scope. A failure or overflow ends the invocation;
no looser retry. Output is positive-only caller metadata, not caller enablement.
No target execution or live/profile/save/Cloud access is included.

## Static output capture correction

The first static scan completed, but its row array was not persisted before the
tool display truncated it. Only aggregate counts68/791/31/45 are retained as
accepted facts; no caller row is enabled from that response. See the first
[scan result](PHASE_1_EVENT_CARD_CALLER_RESULT.md). Nothing is reconstructed.

R and coordinator accept a separate capture-only scope at SHA256
`05325cefd2ccaebb43cb59f96b9c443eed4c0c8490fa6a4ece5dfe0db193d8b3`,
wrapper `8b6899083257009e200bf3b35937ed28921d7de247f0f8545e85cff7bcee540c`,
fixture runner `7e142d8d39543d2282700d69827dda1ad967b84e6cd5ef0cbd940a1ac1740a7f`
and manifest `af38247cca15d9d3e803d165f2a65a975970662374427ae14c5daedc009c084e`.
Its four inert checks passed. The coordinator explicitly authorizes B to perform
one fresh static invocation through this unchanged scanner and new bounded
capture wrapper, using create-only output directory
`/private/tmp/event-card-callers-capture-7f813682e42542f992c82401`.
The wrapper must persist the complete bounded metadata result before printing
its summary. No retry follows a failure. This is a new static read, not a replay
of any gameplay action or reconstruction of the first response.

## Persisted caller scan and generic implementation checkpoint

The capture command initially referenced an absent checkout-local interpreter;
it exited before the wrapper or target opened and the create-only result path
remained absent. R verified that boundary. The coordinator approved the corrected
interpreter path without changing the scanner, wrapper, target or output scope.
The subsequent capture passed and R independently accepted all retained bytes:
38,094-byte result SHA256
`c6340b67d61bd01f447bd4bad320e3ef26abf323a04b1db51f4a0050dc245f6e`.
It contains 76 positive callsite rows: 31 selector and 45 effect calls across the
68 selected event types and 791 scanned bodies. This is positive discovery, not
proof of all branches or permission to enable any new production caller.

R reviewed the generic core, encoder and Cheese-only policy catalog. The 19 core
checks pass, including required card-child completion and disabled known-card
options stopping as unsupported. Accepted implementation snapshot:

- session `8f85f6cf27a336752f7343b82de8588fab289abefcfdda4eb1ae3a4fa013610e`
- encoder `cc35a15348dfc5e1a5b25430700700063eb8c1671ebf248058f6ea32d1a218a7`
- catalog `2557da3e1fec57e63840a973df6804205bb9561a33628f4f0eced3727267e777`
- core tests `7f34c54ddacfb81671aa9ef768a084dad07df0a98d5b4c2eede8799fc5fc101f`

The coordinator accepts this row-free implementation checkpoint. A additionally
owns the new `broker_tests/` directory and its project to test the actual frozen
card/item brokers with the new parent core. These tests may use inert descriptors
through the compile-time test seam; they do not create production policy rows.

The new wire project compiles with zero warnings and errors. The Python host
passes 53 tests: 37 predecessor regressions plus 16 test methods for synthetic
add-three, remove-two and upgrade-two policies, fixed parent domain binding,
preview/history/result rejection, known-unsupported legality and public catalog
closure. R's independent wire/host source review found no blocking issue. Actual
wire negative tests, broker composition, native callers and cross-language
integration remain pending; this is not functional or live acceptance.

## Generic broker acceptance and integration ownership

R and coordinator accept A's actual frozen-session broker composition: 12 checks
pass with zero build warnings/errors. Its source and project remain in
`broker_tests/`; the final aggregate will bind their complete hashes. It covers
add-three, removal-two preview with cleared native highlights, upgrade-two with
delayed effects, wrong result/count, known-unsupported no-dispatch, cleanup,
reentry and sequential card-to-item continuation.

R owns new `integration/` and `integration_tests/` for actual C# service to Python
provider flows against inert adapters. A owns `derivation/` for explicit checked
transformations from accepted frozen sources; coordinator retains aggregate,
checker tests and living docs. These assignments supersede the earlier general
coordinator ownership of derivation work. Contract, API and schema bytes remain
frozen. Generic transform validation fixtures are allowed through test-only
catalogs; they do not enable a native transform policy.

## Retained selector evidence affecting native admission

Coordinator re-read only previously retained, hash-checked selector outputs from
`PHASE_1_CARD_SELECTION_API_SELECTION.json`. Upgrade and simple-grid reward
helpers can bypass their selection screen when the complete eligible domain is
no larger than the minimum and manual confirmation is not required. New native
rows must exclude those cases before dispatch unless a separately accepted
direct-effect path is implemented; generic child support alone cannot admit them.

The retained `NDeckUpgradeSelectScreen.OnCardClicked` body shows multi-card
preview creates upgraded clones and clears original grid highlights. The
retained public `CardModel` metadata exposes `CloneOf` and `IsClone`; their
implementation is not yet part of the accepted body evidence. This is a concrete
candidate for proving preview-to-original references, not accepted native
mapping. Single Smith preview's exact original getter remains separately proven.

## Exact callback evidence invocation selected

R and coordinator accept the revised five-caller callback scope SHA256
`df86ffaf26c8102a4c531a590e16d9dfbbbb90e0af1215b94161be7e34218dc7`.
It supersedes the unexecuted all-string-redacted draft. Exact selected callers
remain Brain Leech, Wellspring, Zen Weaver, Aroma of Chaos and Sapphire Seed.
The revision adds narrow option-key/delegate/constructor offsets and only the
exact removal wrapper, generic selector state machine and direct helper callbacks.

Accepted inputs are Program
`30c08525ccb9247f2064b303b2357d4a3f28a37b39f8a52b61ab4c28fbf3e937`,
12-input manifest
`cd1bbd52730a5ce9ed5ec98ba98ab351508bf2e0087ffdd7dc51c951b2ab7762`,
64,000-byte scanner DLL
`3da08f6d993fb58ed1cd90d240f97d68cf15fdbad4f45b07ad6557b7a57d3ee9`,
capture wrapper
`893873ead8ebf5e971bce8fcb7e53742299563880e08e1ccf05f6d822cbbf9e1`
and capture manifest
`ca4adf369f06048e1073968ae2e4b556ce3210f609e70980d819faba532e3857`.
The scanner and capture synthetic gates pass 12 and 3 checks respectively.

The coordinator explicitly authorizes B to execute one invocation under those
exact pins against the already selected `sts2.dll`, using create-only directory
`/private/tmp/event-card-callbacks-capture-6aee91bd788b4f6ba8ff51c4`.
Persist the complete bounded result before printing a summary. A failure or
mismatch stops without a looser retry. No assembly execution, live action,
profile/save, Cloud or network access is included. The result still requires
independent key-to-delegate-to-selector and effect-completion review before
any new production row or native adapter is enabled.

## Wire/host and derivation source checkpoints accepted

R and coordinator accept generic wire/host source at these exact hashes:

- wire service `a68120c254e471348b9295c79c4a3a28af3068dd19f6e3fe96beb1a6cb560df4`
- wire validation `e4497f4211a52badbbe9c5229fe0e6af47775923219743e1972cd848964dc57a`
- wire codec `5206aa20089894e70b8e1bb419852460c6fb85753c81b734a82b7bfefa41b5a2`
- host `532bf30466e3eb1bf93f6cf6e892e69d0eeeb4bd0894dc6a2d53332735c3b0b0`
- wire service tests `78a9a496e8b598c6a3d144bf0862b60432c5674a44db002b2617c77ce5f37691`
- generic wire tests `c5d8f0ad44d18f1d2964d0f0452fac0d3fa5dd68d76da454f341841891901cc7`
- host tests `32c820d38288b0cb605d4f53c8d4126705b5d4e63714eb0846bf2728bfd3ca19`

The wire suite passes 39 checks with zero build warnings/errors; host passes
53 methods including all four generic operation subcases. This remains synthetic
validation and does not enable additional production policies.

Coordinator independently reviewed and accepted A's derivation machinery after
corrections for canonical paths, complete pre-read byte budgets, unique output
classification and isolated Python imports. Checker SHA
`a25fe0b3c773a873aa4caa61eada5b821d7e9cef31781cc62df879b81c55c673`,
manifest SHA
`4c02f1fa93acf0a4fefd469a262075264777b2213d3138848716dbf8e8cef5c9`
and synthetic runner SHA
`f2e73a6558802e6e56046c36a20520c38d87a31ab1f950ca9112dde03e481c0f`
pass 245 derivation integrity checks and 21 synthetic boundary checks under
`-B -I -S`. These are source-integrity checks, not additional gameplay tests.
Native derivation remains explicitly pending. Root's aggregate project/authority
boundary suite passes 25 checks; its project list remains deliberately empty
and fail-closed until the final implementation is reviewed.

## Callback inspection stopped without caller evidence

The one authorized callback invocation failed with `ambiguous_metadata`.
It persisted an empty result file and a 68-byte fixed error record (SHA256
`12def444279fc4eeb9d7b57d18d394d4bca3be0a7ca73e98c870e28633b52a44`)
in the selected private directory. R and coordinator accept this as a correctly
stopped non-result. No option binding, caller policy or native support is accepted
from it; no target assembly or gameplay action was executed. There is no retry
under that invocation's disposition.

B owns offline diagnosis of the metadata-index ambiguity and a reproducing
synthetic fixture before a separately reviewed correction is considered. A owns
an independent bounded preview-mapping evidence proposal in
`PHASE_1_EVENT_CARD_PREVIEW_*` and disposable tooling. Neither assignment grants
a target read; each needs exact review and an explicit coordinator disposition.

## Actual service/provider integration checkpoint accepted

Coordinator independently reviewed R's integration project and both fixture
sources. It source-links the actual new core/service and frozen card/item
brokers and sessions, with inert native adapters and test-only descriptors.
The nine integration checks pass for sequential add-three, removal-two preview,
upgrade-two preview and transform-two preview, followed by Proceed/map handoff.
A mixed flow proves ordinary transition, item reward and card reward. Negative
cases cover provider mutation/reentry/illegal output, a lost action response,
stale action identity, wrong receipt and wrong child correlation; no uncertain
action is retried, buffers clear and every created card adapter is disposed.

Accepted sources:

- C# fixture `acdb1acbb10e8811fa8889824fd959421fd575fe256968ac2803adefa89ac637`
- project `7723d67efe257016f5b54998d1cf7f2197fc60bf917df4b00a05bd2a298b4b7e`
- Python runner `f3e69d7b77935c78352483109c63b600f9a311b187b1eae854d4cd606a0a16be`

The initial isolated Debug build has zero warnings/errors and DLL SHA
`280399515b288a81fab9980f07e2dce3c185b3d92bb2772d57e59f324ea2d1ac`.
Coordinator hash-verified that artifact and independently reproduced nine checks
under isolated Python. The final aggregate must build and run Release from its
complete pinned source snapshot. This accepts generic composition, not native
caller support or a release. The production catalog remains Cheese-only.

A is also closing R's additional derivation role-collision finding before the
final machinery freeze: an output must not be reclassified as a direct input,
authority or another transformation's original. Current manifest entries do not
use such collisions; the new negative tests must prevent future ones.

## Corrected callback inspection selected

The indexing defect was reproduced without target access: a global dictionary
rejected unrelated legal generic-arity overloads. The correction requires
uniqueness only for the exact selected methods and preserves selected-method,
state-machine and callsite ambiguity rejection. Its 13 scanner checks include
both unrelated-overload success and selected-method collision rejection; the
capture checks remain three.

R and coordinator accept corrected scope
`faca6e77b28471e2a83fce08bd14b388ad3d4a1419cdc3bf3c47026bb55ef5a4`,
Program `2e1dabcff216f62fe318c6745a49dcae19f5af55af2dda99b011beba9354f5d2`,
13-input manifest
`a3a012d287f73f37ff1865417e0ff30363ea375b319b02906993670b8f6c6851`,
64,000-byte scanner DLL
`f1c728be934b5b2e462618634abfed18b613384df7eabd3d01570ce35ac63ac7`,
capture wrapper
`f7d4eca3aa729ad15f5ef3075ffb26f43ea2866c6fa89870e0588ab9497fa66a`
and capture manifest
`cac346c367cea8409033d49aa5bc7267e9e161cf240a042fdb5b1bcf88069d59`.

The coordinator authorizes one new corrected metadata invocation under these
exact pins into create-only directory
`/private/tmp/event-card-callbacks-capture-a5c9bfe32eb74388a0cff59d`.
The same selected five callers, helper chain, bounds, pinned target and private
persist-before-summary rule apply. Stop on failure without further invocation.
This does not replay a game action or enable any caller. Result-level native
policy selection and completion proof remain separate gates.

## Callback diagnostic evidence capture selected

The second callback invocation stopped with `option_binding_mismatch`; its
accepted attempt-2 result is
`b1757b24a146d53df96a8ff49283ac18c8289ba193b53e61ce605057203046ce`.
No body or option row was accepted from that empty result.

R and coordinator accept the separate diagnostic scope
`58f91c813420ac6e319b80bfaaa9db73291601d68ffab7741a529e4f87ae2921`,
Program `f2bdadd1b4f8bc6f32253195b506631704e9c11fa751df407e3e5b86263cd64e`,
source manifest `3a64ea4f4ede20e35035adf9b40f55f8978a18bef8a41eb929ad73db3298d5d5`,
71,168-byte DLL `3c84de8f20d178875b8e2c34d7c90c19063e7462de4c25fe9fe3f23909df566a`,
capture `eb1d9d92c17278845f29f6bc1106af5551b2af35a9b8362e795547844414e8a8`
and capture manifest `d2b1ce75ab3da77443c58776a4afaf0e05fb75dfadeb554000574dbb2a826dca`.
The 15 scanner and three capture checks pass. The producer retains bounded
bodies even when no direct constructor or no unambiguous binding exists.
Lexical and unresolved rows are evidence only, never support classifications.

Coordinator GO: one new metadata-only invocation into create-only directory
`/private/tmp/event-card-callbacks-capture-08be47c191934886a0103cd5`,
under these exact pins and bounds. Persist bounded output before summary, stop
on any failure, and do not invoke again under this disposition. No gameplay
action, profile/save access, target execution or native row is authorized by
this diagnostic. Exact caller selection remains a separate reviewed stage.

## First exact native caller selection accepted

R reviewed the full callback evidence and coordinator selection. The complete
diagnostic result e8c6f9aad5035daa21e7329b89669c652c9845ae07049d9f1274b1cd26c762ca
contains26 bodies/1316 instructions, nine binding candidates, two unresolved
constructors and14 lexical candidates; only independently traced rows are used.
The exact selection record is `PHASE_1_EVENT_CARD_OPERATIONS_V1_CALLER_SELECTION.md`,
SHA-256 `e34a7190409c99ffed1693cd4af8c6ae37f50932b1c93be093927ae4188cf492`. R's requested explicit public null-selector
guard replaces the draft's vague nonlocal predicate; private selection helpers
are never invoked.

Stage2 GO: implement only AromaOfChaos MaintainControl and SapphireSeed Eat as
exact upgrade1/PreviewThenConfirm rows with pre-dispatch upgradable deck domain
2..64, exact selector/task/preview bindings, and same-instance IsFinished after
the exact upgrade effect. Known paired unsupported options and all other
positive selector types remain fail-closed as specified. No add/remove/transform
native row is selected by this disposition. No release or live action follows
from functional implementation approval.

Exclusive ownership: A owns core catalog and native/EventCardOperationBinding.cs,
native/EventCardSelectionV1NativeAdapter.cs, native_card_tests/** and derivation/**.
B owns native parent adapter, production child factories, closed native registry,
native project and native_tests/** after its bounded diagnostic extension.
R owns independent review plus existing integration/** and integration_tests/**.
Root owns host/wire consumers and tests, aggregate/checker, README and living
acceptance/status documents. Shared contracts, all predecessors and old48 remain
byte-exact; additions to the selected rows need a separate exact disposition.

## Preview identity candidate inspection selected

R and coordinator accept preview scope
`3546c522bcbce17dfdcb18daca8dd1531514f6c5b1f87a28d38368f5e3a88bb7`,
12-input manifest `57a66b5d69c19c2c01aadff7c71dcca1fd3b9972a9f14687c6a9ca0aab93972e`,
Program `abf480ba2b3705ed9f8a0982718705a1221aa4087add89e12a54e35bd202c53a`,
capture `8f823279dc59938b87d6e30d8753b40259343878613beb583db211d1be1cbb49`,
and twice-identical55,296-byte DLL
`caa81357e3c1a212e7999ab9d51267366ccc8c7fbe1dafa12a7362f819b5745c`.
The exact21 inert checks pass; coordinator reverified every manifest input.
This bounded evidence producer preserves candidate handles and interface maps
without asserting CLR dispatch resolution.

Coordinator GO: one metadata-only invocation under the exact scope into absent
create-only `/private/tmp/event-card-preview-capture-235342f6df5b4c1ba424a6aa`.
Persist bounded stdout/stderr before validation. Failure ends this invocation
without a retry under this disposition. No native multi-upgrade/transform row
is enabled by it; result review remains required. Root will execute the packet
so A can continue the independently selected native single-upgrade work.

## Production host rows and result identity accepted

R independently reviewed the two accepted production policy definitions and
reproduced58 host tests. The exact host SHA is
`45e3c0607240dbf18b3e18c7e6c485b38766c744050449af794e622e35100f7b`;
test SHA is `27fc36161f29f3390bcf2063df3bbc748956755f35073eac52b0284a080c2f97`.
Tests exercise public run_event with both rows and domain2/64, exclude
selectorless0/1 and over64, reject paired unsupported keys and wrong selection
counts, and reject substituted resolved originals after accepted selections.
The last case exposed and closed a host-only missing selected-slot equality
check; no Proceed or extra POST follows the invalid result.

Default full-repository pytest initially stopped at collection because the new
host fixture reused the frozen predecessor's basename. Only the new fixture
was renamed to `host_tests/test_event_card_operations_host.py`; the aggregate
pointer follows it. No frozen file or source contract changed. The broad
regression is being rerun with the unique name.

## Preview inspection outcome

The one preview invocation failed the wrapper's canonical-byte comparison
because System.Text.Json escaped one plus sign differently from Python. Its
bounded complete stdout remains private and hash-bound (14,163 bytes,
`587b2ee8484f2e372fb317416c94f048e47833d2e35f984e6a75dc62850e0857`),
with empty stderr. R reviewed those existing bytes and all nested schema/counts,
without a rerun. The result record
`PHASE_1_EVENT_CARD_PREVIEW_RESULT.md`, SHA
`c7bbf73e0aa783a5fe1fda7f26d6613079f27c1fd89d63e2059d00ecea4dec63`,
records this as failed capture with complete diagnostic evidence, not a scoped
success. The candidates do not prove CloneOf original mapping for multi-upgrade
previews. No native multi-upgrade row is enabled. Current single-upgrade rows
use the accepted public NUpgradePreview.Card original and are unaffected.

## Broad regression and production wire checkpoint

Default pytest now collects without duplicate-module errors. The full run
reported1300 passed in108.45 seconds and one existing inert transport fixture
blocked by the sandbox's localhost bind restriction. That exact unchanged
fixture passed separately in0.01 seconds with the local socket permission.
No game bridge listener or external service was started by the fixture. Thus
all1301 collected checks passed across the full run and its focused retry.

The production wire test extension initially passed51 checks after a zero-error
Release build. R requested an additional positive domain64 boundary alongside
domain2 for both selected rows; the final test source
`c4a1235f94e8905f3e6305378169b2dff06bf180ebe368ef69779562915c48a9`
is independently source-reviewed. Its logical count stays51; execution of
that latest source remains required by the final aggregate. No wire production
logic or frozen schema was modified for these policy-row tests.

R now owns the disposable BrainLeech/ZenWeaver follow-up evidence producer
and its two new PHASE_1_EVENT_CARD_FOLLOWUP documents; root/B perform its
independent packet review before any coordinator-selected target invocation.
This lets A and B continue their disjoint native implementation files. No
additional caller row has yet been selected.

## ROOT GO: Brain/Zen bounded follow-up — 2026-09-06

Root independently reread the exact scope, scanner and private capture wrapper; B independently accepted the same packet. Scope `c5c378d83fd6b550c13e55f10e1c93c4f6046afc4ffe563185f0d90f7957d834`, validation `5dc0001792ca092fc4358483b9d367411ea4d0dd7b1a9d6413c404f470dd2c2b`, scanner manifest `3a5745be55c139af6004d352c352cf8c043bb8feaf859f4ba02ebbc1f39fc370`, capture manifest `18b76763b7dfc57ef93261ce7510ac7d2b3dd6c708e02efaebc6980343b5619b`. Source entries independently hash-verified. Scanner 15 and capture 4 inert checks passed. ROOT GO authorizes one exact metadata-only invocation through the reviewed capture wrapper, with complete bounded output persisted in a fresh private temporary directory. It reads only the pinned game assembly, reveals the selected Brain generation-count key and two Zen callback bodies, and cannot itself enable production rows. A failure ends this invocation; no broader read, live action, or retry is authorized by this GO.

### Follow-up pre-target failure and separate corrected bundle-path GO

The first capture invocation returned `file_shape` before output-directory creation or scanner execution. Root selected `fixture-run/target-tool`, the inert config-mutation fixture: its selection was 2,898 bytes, SHA `09dbdf606e707f3a14b7a4fb73d917537334bc72623dabfba1b619962bc2129d`, and failed the exact 2,897-byte bundle gate. No target read occurred and no result was reconstructed. The attempt is closed.

Root separately verified all unchanged source and bundle pins at `/private/tmp/event-card-followup-b/fixture-run/tool/bin/EventCardCallbacks/debug`, including the accepted selection SHA `cf9e66383f9d41b8a061cb3ad8544a2e0b1a4dff43ca0744723962257c0a5a31`. ROOT GO now authorizes one new capture with this exact reviewed bundle and unchanged scope `c5c378d83fd6b550c13e55f10e1c93c4f6046afc4ffe563185f0d90f7957d834`. Destination is a different fresh private directory. This is a corrected pre-target path selection, not replay of a target or gameplay action.

## Native two-row review and complete follow-up capture

Independent A/B/R review accepts the two selected upgrade rows after repairing exact selected-key/type recapture and rejecting a foreign CardSelectCmd override even after the selector closes. Native-card11, native-parent14, factories4 and full native-composition2 pass. The production compile-only assembly is 72,704 bytes, SHA `c8f1d891489ae51820dd4f48eef67c62c76d4f41cedd1d5b9d1078424e98f3aa`, with zero warnings/errors and no copied target references. It was never executed. Final aggregate/source freezing remain separate.

The separately authorized follow-up capture passed at `/private/tmp/event-card-followup-capture-841a406cc84f4e879d130eb1/result.json`, SHA `8fd76338f6b6eb64aa1063fa24b5cf77fd66fbaeec062af234bea09186e7cfe9`: two complete Zen bodies, 140 emitted instructions, 291 inspected instructions including the non-emitted Brain body. Brain's one revealed code key is `FromCardChoiceCount`. The result requires manual semantic/row review and enables no additional native policy. The prior pre-target bundle-path failure stays separately recorded.

## Functional candidate accepted; first source freeze authorized

Root and independent R review accept the final checker literals at SHA `20de6604f790feca0e3651735d785aa30c523dcc72a4a831f72e962e624b81e7`. Candidate aggregate `/private/tmp/event-card-operations-b-check-a/result.json`, SHA `c7b81fc4a1b55c5f515d0f608e9ac019bc8009b09bd0c3d50a33b76c474b12ff`, passes 675 reported checks across 19 suites, including derivation304, exact11 project closure, actual service/provider integration9 and full native composition2. Two clean Release native builds match at 68,096 bytes, SHA `2773b2a2d197a9fee82dce8488a0c6752d6dc4db0b67f8eb90d20769438f9577`; no target assembly executed. These are Release outputs, distinct from the earlier Debug checkpoint. Independent derivation synthetic24 also passes.

Root compared the entire candidate source snapshot with current source: only README documentation changed. R separately accepted README SHA `7fbc39e9a9e1c1848ad88750737581a79275c0ed2d617ac9e99625f86893ce` and the final derivation manifest `4c0f693b75ee1c498ef962081636e6c862e811d27d7835592db85438b1f59a95`. ROOT GO authorizes first source freezing of this exact three-row functional component and its frozen aggregate. The catalog remains Cheese plus the two selected event upgrades. Brain/Zen evidence does not silently add rows. No installable release or live campaign is created.

## Final functional acceptance — 2026-09-06

Root's frozen aggregate passed at `/private/tmp/event-card-operations-root-frozen-a/result.json`, SHA `4ac1377eeb96c1e94c986002c983dadbf6d29ab722b122b3f3f456dc74490cd7`. All 675 reported checks across 19 suites passed against frozen source. The 66-file source manifest SHA is `8e5d4ef4701929f2543a09ac41875c4479b59bf98e37286685cc86cbb5df053d`; source inventory SHA is `97735acc3534bbe1841032dd10a361567b3b6f90e008992b2ab2fd3718fd7967`. The candidate and frozen gates together produced four byte-identical Release native assemblies, 68,096 bytes, SHA `2773b2a2d197a9fee82dce8488a0c6752d6dc4db0b67f8eb90d20769438f9577`. All thirteen predecessor inventories, original48 and both pinned compile references passed; target assemblies were never executed. Independent derivation synthetic24 passed separately.

Functional development of this component is accepted. It implements the shared event-card domain/capability contract and exactly the preserved Cheese row plus Aroma/Maintain Control and Sapphire/Eat upgrade-one. Repository regression covers all 1,301 collected cases across the full run and one unchanged sandbox-only localhost fixture rerun, as recorded above. No release listener/package/installation or live campaign was created, and no all-event or new live coverage claim is made.

Next concrete development is the separately proposed Brain/Share Knowledge generated add-one row, then Zen removal after exact argument/key binding and transformation after authoritative replacement mapping. The accepted component remains immutable; the future-row proposal does not enable a row. The complete Brain/Zen metadata result is independently reviewed with the Zen argument-order uncertainty explicitly preserved.

## Post-acceptance user clarification — generic discovery priority

After commit `b2ae0dd`, the user clarified that the intended all-event system
should handle supported interactions without registering each event separately.
The coordinator acknowledged that the completed work mixed generic mechanics
with individual native connections, and that the exact caller allowlist remains
a limitation. No accepted source, contract, result or evidence is changed.

The [generic event handler plan](../PHASE_1_GENERIC_EVENT_HANDLER_PLAN.md)
supersedes this ledger's former next-Brain/then-Zen implementation priority.
Those proposals remain retained evidence and candidate tests. Next is discovery/
admission contract design and shared family support in a successor, preserving
pre-dispatch accounting and authoritative identities. This is a documentation
handoff, not a new implementation, static invocation, release or live campaign.
