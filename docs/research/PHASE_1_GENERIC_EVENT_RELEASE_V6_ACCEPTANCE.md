# Generic event release v6 acceptance

2026-09-08. Offline release accepted; one user-requested live Search invocation
completed and its campaign is closed. Parent continuation passed, but no item child
was exposed, so live item acceptance remains unmet. All26 predecessors stay frozen.

## Accepted scope

[Contract](../PHASE_1_GENERIC_EVENT_RELEASE_V6_CONTRACT.md) SHA256
`d23e886df85e88ac3fce3f19fb35199580c54825edb066afba52ff0355f074ed`, independently
reviewed by R before implementation. Source-link exact G6 gameplay and frozen
releasev5 diagnostic enum/codec into a new protected release shell. Root owns
production/checker/client/package/operations, A runtime/transport/socket, B native
bootstrap/operator/lifecycle, R independent verifier and production policy.

Canonical JSON request cap512bytes and HTTP request-head cap1024bytes remain.
Longest valid item request248bytes contains two64-character decision IDs; the
longest head481bytes also contains64-byte authentication. Item/transform resolved
responses remain nonterminal. Cumulative card/item completion counts survive
Proceed. All78 diagnostics remain cached parent-capture evidence, not universal
child-failure explanations.

## Live candidate, fixed policy and setup

Retained UI in PHASE_1_EVENT_CONTINUATION_LIVE_ACCEPTANCE.md observed Room Full of
Cheese's lower Search choice as damage plus a relic. Rehashed retained
GenerateInitialOptions metadata SHA256
`bf65b5ada4aac05e78798fd2884a6e88050749dbcd48bcd3d09cb2f7b602e8dd`
confirms Gorge index0 and Search index1. CanonicalVars SHA256
`f0d6ec9ee681b1bacffe776112400e114d049d51da9053ad94bf52390e89da5f`
sets base damage14. Search's body is not retained; no singleton Offer behavior is
assumed. The upcoming attempt is intentionally bounded and stops on an unsupported
request shape. Historical standalone Loot tests cannot establish generic ownership.

After installation the user manually prepares Profile3,single-player,fresh Room
Full of Cheese initial choices. Require current HP strictly greater than the
currently displayed Search damage cost, an empty potion slot, and no console,
selector,reward,map or other popup obscuring the event. Leave both options
untouched. The client requires exactly two initial candidates and legal choose:1,
selects Search once, then uses first_legal for children and later parent choices.
No event-key admission catalog is added. The expected successful result is one
verified item child and explicit Proceed/map handoff; this is a test expectation,
not prior evidence. Exactly one client invocation; no retry/adoption after doubt.

## Offline status

Focused root checks:13 client tests,50 predecessor conflicts,41 transactional
campaign cases and17 process/port boundary cases pass. The first invocation of
client fixtures used system Python3.9 and correctly failed the production3.10+
version gate; supported project Python passed after updating new counter
expectations. The initial sandboxed loopback fixture could not bind; the same
fixture passed under the authorized local-socket test scope. No game was involved.

Focused A checks pass:843 runtime assertions,22 Python transport tests and30
socket/client scenarios (17 preserved plus13 G6 additions), all with actual native
sources where claimed. Public campaign policy, potion/relic, three delayed task
roles, late effect invalidation/fault, repeated items, mixed item→upgrade→transform
→item→Proceed, transformation and fixed multi-upgrade are covered. Each scenario
uses run_live.run_once with synthetic credential/state dependencies. The mixed
fixture requires5 parent+7 child actions,17 reads,2 completed cards+2 completed items.
B checks pass:operator4,bootstrap13,native39 including all18 hooked targets.
Root package5 tests/9 mutations and clean-install7 fixtures pass.

Two fresh production builds are byte-identical,379392 bytes, SHA256
`96723ba26f9f64a74cd0f1d8ae4217191a13410c5629a1feedc26a44f3890c2f`.
Production-pair result SHA256
`8c856514ebf76a956c4d884b42466c7bd5bbff8328d5ebdfcee116cf1cb7d89f`.
The352-byte manifest is
`c7480a29c041269b9b0991826fa2fd4408cc8673a4427dac9ca299c9396ece62`;
the380172-byte canonical stored archive is
`ecc08551b583fdc64e3fab87bbf6bf3a0ba09ef9e8d5521a824261ee6c97ddd3`.
B independently reconstructed/rehashed the package and checked every operation pin.

R's128 verifier checks pass, including28 new checks. Both independently extracted
policies reproduce exactly:1617430 bytes, SHA256
`7da3d689a9fa9838d9f6f8fccd0dd5ec6f8e939d1ff769d443dccc008ce1cb4c`.
Coverage:56 sources,2112 method bodies,14526 metadata rows; source projection
`ead097f742daf97e185dd897f50926e5f98f14a7211c1ec278df4e15e986e830`,
metadata projection
`8ac73c681f743be938f208e1848862489ca47a7ad535e33ab64a4b8051115a67`.
R result SHA256 `1aa924b93b9626c2098bc316b63ea5e7b6e3879f8ff3bfb4b35a37019944b32f`.
The verifier required exact constructor-only reflection allowances for G6's
signature checks. Its inherited unescaped metadata cells also rejected the pipe
in compiler-generated local-function names; percent-first canonical escaping of
percent,pipe,CR/LF fixes representation while preserving strict token/body linkage.
B independently reviewed both narrow corrections. Production code did not change.

The first aggregate invocation stopped in source preflight, before scratch/build,
because inherited repository-source reads capped files at1MiB. The new policy and
2.97MiB derivation exceed that. A new release-only4MiB bounded regular reader now
reads own metadata with no-follow/nonblocking open, bounded chunks, EOF and
pre/post descriptor/path identity checks. Frozen readers/caps remain unchanged.
R independently reviewed its sole call site and seven disposable boundary cases.
The same still-nonexistent candidate scratch name was then used for the corrected
first aggregate execution; no live action was involved.

Both full aggregates pass:

- Candidate result:97cb36fe13d83a6b22efc474222ce4e6ff0401e852b965f82f4cbd7f30bd8c13.
- Frozen result:bb8786ad4e6e6f0abe691f1bb24e9640a070dd0414d391d42a374cb4b91bb94f.
- Frozen inventory:cde4b3a0edae8ee38801aa5b74f089ec1da782dbfd387d5e751edebe5ca5137a.
- Source manifest:6430fed7761f5336ff61bc92b91229d6b75567175ce149d6834cbcf4e118ade5.

Each gate includes the full G6 regressions:3199 native,1718 item,879 transform,
147 wire,110 host and161 integration cases (99 actual-native). Each builds the
production bridge twice identically and checks all release suites listed above.
The frozen gate additionally validates the client source manifest without reading
credentials. R independently rehashed candidate/frozen sources and results.
The new component has65 inventoried files plus source_identity.json,63 exact
derivations and52 reused C#/Python sources. All26 predecessor inventories and
the original bridge remain unchanged.

## Preserved installation and pre-live instructions

After both gates and independent review, root published the exact canonical
three-file artifact directory. Fresh require-stopped passed (3 process samples,
2 closed-port samples). Base verification passed429files,zerooverlay,projection
`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
One fresh transactional install passed, phase installed, mods_parent_created true.
Installed state SHA256: `def7e9ae464f16918b6eb28b936a43f95e80fd9aba51f843fb73342448d167d0`.

Post-install require-stopped and exact overlay checks passed: unchanged429-file
base and exactly2 overlay files. validate_installed_for_client passed for that
state with credential content unread. No game launch or client invocation occurred.
The source-frozen package is ready for the manual setup described above.

After user readiness, root verifies current UI and require-running, then exactly
one invocation:

```sh
/Users/rowdeygoos/code/github/RowdeyGoos/StS_agent/.venv/bin/python -B \
  /Users/rowdeygoos/.codex/worktrees/23cf/StS_agent/bridge/Sts2AgentBridge/successors/generic_event_release_v6/client/run_live.py \
  --expected-state-sha256 def7e9ae464f16918b6eb28b936a43f95e80fd9aba51f843fb73342448d167d0
```

Preserve bounded summary/diagnostic only; no retry or manual child selection after
uncertainty. Success requires a verified item child and explicit Proceed/map.
Quit normally and complete stopped/closed,code-first quarantine,exact owned purge,
clean base and final stopped/closed checks. Use each newly returned state hash for
its next cleanup action; the installed hash is not a later quarantine hash.
Repeated unmodded launch remains waived. No profile/save/Cloud work is authorized
or performed by this campaign.


## One live invocation and completed cleanup — 2026-09-08

After the user's ready message, CUA verified initial Room Full of Cheese choices:
HP62/80, Search cost14 and the offered Chosen Cheese text, empty potion slots,
deck15, no console,selector or popup. User readiness supplied the requested
Profile3/single-player setup; the in-event screen alone does not independently
identify the profile. Require-running passed with one exact process sample.
This mode reports process state, not a listener probe (port_samples0); successful
client exchanges provide the subsequent transport evidence.

Exactly one frozen client invocation returned exit0 and the following bounded
summary. No uncertain POST was retried and no extra gameplay actions were taken.

```json
{"schema_version":1,"status":"resolved","parent_attempted":2,"parent_accepted":2,"parent_reconciled":2,"child_episodes":0,"child_attempted":0,"child_accepted":0,"child_reconciled":0,"total_attempted":2,"reads":4,"effects":"unverified","completed_card_children":0,"completed_item_children":0,"code":null,"last_response_diagnostic":"map_ready"}
```

CUA then showed the map, HP48/80, an additional cheese relic icon, and unchanged
visible deck15. Thus the release completed Search and Proceed/map. No item child
or collection action was exposed to this controller: child_episodes0,
child_attempted0 and completed_item_children0. The singleton item acceptance gate
therefore remains unmet despite the overall resolved status. Effects is unverified;
UI observation does not substitute for the item engine's reward-local witnesses.
This does not establish the exact underlying Search grant implementation or prove
a generic item-hook defect. The retained Search body was not inspected. Next test
selection should require evidence of an ordinary singleton reward screen rather
than infer that surface from visible relic gain alone.

Root requested normal UI quit via Command-Q; CUA reported App quit. The subsequent
wait-stopped passed (3 process samples,2 closed-port samples). Code-first owned
quarantine passed using the exact installed state, returning quarantine state:
`85d27c79f2e74f377b45d5924238046fadcb0808592c0b89c1a57f92d149f6e6`.
Purge used that fresh hash and passed: phase absent,4 generated files removed.
Clean-base verification passed429files,overlay_count0,projection
`d111d988aca63d8933b8b88968f4e3ecd8006e877eb2990e60b8a40511c50be0`.
Final require-stopped passed (3 process samples,2 closed-port samples).

Campaign GENERIC-EVENT-V6-SMOKE-V1 is closed. No installed overlay,credential,
operator state or cleanup remains. Installed/quarantine hashes above are historical
and must not be reused. No automatic launch,force kill,new target inspection,
profile/save filesystem or Cloud work occurred. All frozen source identities remain
unchanged; the existing offline evidence is preserved without redundant reruns.
