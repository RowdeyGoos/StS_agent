# Generic event v4 acceptance ledger

2026-09-08. Functionally accepted, frozen and validated after user-directed
continued generic handler development.
Baseline `e429891` in the authoritative 23cf integration worktree. All 23 frozen
successors and original bridge remain unchanged. All generic live campaigns are
closed; game stopped, no installed overlay/credential/cleanup. No new live setup.

## Accepted contract and ownership

Independent reviewer accepted the exact [v4 contract](../PHASE_1_GENERIC_EVENT_V4_CONTRACT.md),
SHA256 `c8c920769ce80246e3925722716e8b522360d6033b788092f77bbaa36e47a14b`,
before implementation. Its prepared-document header preserves the review stage;
this ledger records its subsequent acceptance for this slice.

The slice adds fixed-count upgrades1..8 through authoritative native multi-preview
mapping and cumulative `completed_card_children` evidence. `effects` retains its
last-parent-action meaning. Multi-upgrade previews bind exact CloneCard arguments
and results under one consumed deferred-click ticket; CloneOf is not a witness.
Clone references must be disjoint from the entire retained baseline/original domain.
Transform, variable upgrade minima, optional/scrolling and custom/item/combat
families remain open. Existing removal/reward and single-upgrade semantics persist.

Root owns contract/checker/projects/dependency/provenance/README and living docs.
B owns native and native fixture C# plus retained native evidence; A owns core,
wire, host and their tests; R owns integration sources/tests and independently
reviews native/consumer changes. SDK lane is serialized B→A→R→root. Changes are
in new generic_event_v4 only; predecessor sources stay frozen. No target inspection
is needed: B rehashed retained native control/clone/model evidence and R accepted
the mapping scope. No target assembly execution or profile/save/Cloud work occurs.

## Implementation and evidence

The functional successor was scaffolded from generic_event_v3, incorporating
v5's corrected native adapters and lifecycle binding. Game-owned pinned Harmony
replaces the functional predecessor's package dependency for inert tests. The
new offline checker verifies all 23 predecessor identities, explicit compile
closure, bounded native references and reproducible compile-only native builds.
The later sections record completed implementation and candidate/frozen gates.
This component has no package, listener, installation or new live evidence.

## Consumer focused handoff

A's core/wire/host implementation is stable. Release core, wire and wire-test
builds passed with 107 wire cases (95 retained plus 12) and 86 Python host tests
(71 retained plus 15). Evidence snapshot: `/private/tmp/generic-event-v4-consumers-a`.
These cover fixed upgrade counts, exact parent-before-child count sampling,
malformed/lost terminal responses earning no completion credit, forged/regressing
counts, duplicate resolution and completed-child retention across later failure.
The child payload and last-action effects semantics remain unchanged.

Root reviewed the cumulative count implementation and added explicit aggregate
guards that all three old card/removal/reward native adapters remain namespace-only
v5 derivatives. The new multi-upgrade adapter's separate derivation is recorded.
Native review and focused/integrated execution remain in progress; no freeze yet.

## Native focused checkpoint

B's focused snapshot `/private/tmp/generic-v4-b-03` passed 1,289 native assertions,
including the exact preserved 745 baseline, and a pinned compile-only native
adapter build with zero warnings/errors. Deferred ticket validation rechecks the
retained grid/geometry/holder/card/hitbox before consumption. Complete previews
retain each holder's exact clone model, rejecting later swaps and replacements.
Additional required-negative audit, independent native review and the complete
native-to-Python suite remain in progress; this is not final acceptance.

Independent root-tool review found two completeness gaps before aggregate use:
script execution would omit the new host test module lacking a main guard, and
reused provenance omitted the frozen Python child host. The checker now uses
unittest discovery and requires the exact external C# plus Python host reuse set.
No earlier aggregate was claimed from those incomplete checks.

## Final independent acceptance and frozen gates

R's final integration snapshot `/private/tmp/generic-event-v4-integration-b`
passed 96 scenarios: 60 pure composition and 36 using the production native
hooks/adapters with inert target stubs. All prior 73 scenarios remain. Both
integration projects compiled with zero warnings/errors. Cases include fixed2/8,
reverse selections, delayed creation/click/preview/effects, foreign or replaced
preview rejection, retained derived reward hitboxes, lost Confirm without retry,
and mixed upgrade→removal→reward with cumulative count3. Independent native and
consumer review found no blockers after the required stale-generation guard.
A independently reviewed the root checker/projects/provenance and accepted the
fixed test-discovery and dependency-inventory gaps.

The complete candidate gate passed at
`/private/tmp/generic-event-v4-root-candidate-a`, result SHA256
`52a1ccb90b6d9151763915a29da9d34e186d60437b112adcb034be5bbcebd7e5`.
The complete frozen gate then passed at
`/private/tmp/generic-event-v4-root-frozen-a`, result SHA256
`764e1462e4e24649415fdd6dc1e4f39170fb84b5cc4167e1fce76ff1915e7f81`.
Each ran all final 1,343 native assertions (including the exact preserved745),
107 wire cases,86 host tests and96 integration scenarios/36 production-native.
Final counts include later stale-generation, preview-swap and new-hook cleanup
cases; the earlier1,289 focused count is retained only as historical progress.
No target, Godot or production native assembly executed. Only pinned game-owned
Harmony executed against inert stubs; both full gates used no network packages.

After independent acceptance, candidate completion and staged whitespace checks,
the 38-file source inventory was frozen as successor24, plus source_identity.json:

- Inventory SHA256: `0edb1e07892d8099a52464b4ccb1d1b155dcfd95d8ea6ff99cab4efb238af441`.
- Manifest SHA256: `1d4353589427a868d418fd7bdcc163106745784ab6ad64d8bfbb4265209d1b6d`.
- Native assembly:84992 bytes, SHA256 `ff5faa7cdd30bfd6ee5e7a57afa28e0f7e3095eae973079aba920be76311e7c5`.
- Explicit production closure:15 C# sources;32 exact source derivations and8 frozen
  reused C#/Python source entries. All23 predecessors and original bridge verified.

Four native builds across candidate/frozen gates are byte-identical. Every owner
closed the G4 source lane; no post-freeze source change occurred. This accepts
fixed-count native multi-upgrade as a functional family and cumulative completed
child evidence. It does not claim an installable release, live multi-upgrade,
variable upgrade minima, transformation, item/optional/scrolling/custom/combat
families or complete event coverage. All live campaigns remain closed.

The next development step is bounded transformation discovery, using the exact
new [static scope](PHASE_1_GENERIC_TRANSFORM_SCOPE.md). Native replacement identity,
final deck ordering and intermediate effect states must be established before
choosing an implementation contract. No old capture or campaign is replayed.
