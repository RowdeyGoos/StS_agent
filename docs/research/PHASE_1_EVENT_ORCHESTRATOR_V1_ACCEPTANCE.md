# Event orchestrator v1 acceptance ledger

Baseline `7e57bb6`, 2026-09-06, selected23cf integration checkout. The user chose
broader reusable event handling toward all events. Smith upgrade-one and the
user-prepared Cheese add-two flow are live-demonstrated; both campaigns are
fully cleaned and closed. No campaign is active.

## Functional contract acceptance

The coordinator accepts
[the functional contract](../PHASE_1_EVENT_ORCHESTRATOR_V1_CONTRACT.md) at SHA256
`da7cdbf6efcb6f2786e59a64a0b0d9ed7f9b355cd966f8a73b61d385987c543d`
after R's independent review. This disposition supersedes the proposal banner
without altering frozen semantic bytes. Review closed the receipt admission
window on intervening transitions and added exact kind-specific pre-dispatch
policy binding plus lifetime factory/screen tombstones across child episodes.

The first slice combines ordinary options, existing exact item rewards and
Cheese card selection under one event parent and provider-driven host. It is a
functional increment toward broad coverage, not all-event support or an
installable live release. All twelve predecessors and old48 remain frozen.

Core API and wire schema are pending independent hash acceptance before dependent
implementation. A owns core/brokers and their tests; B native adapters/fixtures
and separately bounded census tooling; coordinator wire/host/aggregate/docs;
R independent reviews and actual cross-language integration fixtures. No
same-file parallel ownership. No code, source inventory, candidate or aggregate
is yet accepted for this new component.

The separate [coverage plan](../PHASE_1_EVENT_COVERAGE_PLAN.md) tracks the full
objective. A bounded metadata census will establish known event types and
interaction-family gaps; no new target read is authorized by an unreviewed
selector list. The user's selected event-development scope does not extend to
profile/save filesystem, Cloud, retained live corpus, remote Git or unrelated
capabilities. No user game setup is needed during this functional packet.


## Core API accepted for implementation

R independently accepted core/EventOrchestratorV1Contracts.cs at SHA256
`37f37caecb1a439761112b38a36229a25c29a2a674236978ae5f0bbde80b5fb1`.
The coordinator accepts that exact API and activates A's core/broker/test packet.
The retained DRAFT comment is superseded by this acceptance without altering the
reviewed bytes. Review corrected accepted-context timing to after successful
dispatch, introduced a bound transient capture, fixed public strings and required
exact factory/policy/correlation identity. Empty Missing is initial-only.
Wire schema acceptance remains pending; frozen APIs are not silently edited.

## Wire schema accepted for implementation

R independently accepted wire/wire_schema.json at SHA256
`e4da960bf0525b93d1526e01f44c01c190efe408c31b86e865e3082d21c4e399`.
The coordinator accepts this exact schema and activates wire, host and integration
implementation. Review fixed closed failure codes, provider kinds, internal-only
child admission signals and resolved-child delivery before parent consumption.
This supersedes the earlier pending-schema statements; implementation remains
unaccepted until aggregate validation and independent source review.

## Core and broker source review

R independently accepted the core/session and real child brokers after cleanup
ownership and reentrant-disposal corrections. Isolated fresh builds had zero
warnings/errors; the core suite passed 12 groups and the broker suite passed six.
The reviewed session SHA256 is
`7c76c599597a8dc6a5402feeb3ae4710193ba18c798fa415931443c871e12b9a`;
brokers SHA256 is
`ad2490408e6b55d0f3c36ed996a61d41933b41340fb4bea14e74099c561ab7b5`.
The actual frozen item/card sessions are used. Aggregate acceptance remains open.

The separate metadata census completed its synthetic gate and independent scope
review, then its single target invocation stopped on ambiguous ancestry. No
complete event list or coverage denominator was produced. See the
[bounded census result](PHASE_1_EVENT_COVERAGE_CENSUS_RESULT.md).
This does not invalidate the previously accepted native facts used by this seed.
No second target invocation or expanded inspection is implied by this result.

## Host, wire and integration review

R accepted the coordinator's host and wire implementation after strict JSON,
receipt-window, deadline, cleanup and disposal corrections. The required-provider
host passed 37 checks; the actual service wire fixture passed 22. Actual C# core
plus real item/card brokers through the actual Python host passed nine integration
groups, including sequential children, ordinary multiple pages, Unicode text,
missing/duplicate delivery, stale lineage and provider failures.

Reviewed host SHA256:
`2ed7e2b1db36af371c02dfa9643bb28ba26da344bc83838899ed006baed234dc`.
Reviewed codec SHA256:
`e7500d42e46a3e5948d59d7b759e05ffdd7acd21fbb2af6daa4d63ed385abc0b`.
Reviewed service SHA256:
`ac20a721a44ab6d7c58ec371d01366b6245814b118a9b4577240e0c02b0045e6`.

Repository regression passed **1,243 tests in109.16seconds**. The first invocation
stopped during collection because the new checker test shared a basename with a
frozen predecessor test. Renaming only the new checker file fixed collection;
no predecessor changed. The full rerun passed. New C# native/aggregate work still
needing completion is separate from this repository regression result.

The aggregate checker independently verifies all twelve predecessors and old48
unchanged. Its boundary fixtures now pass22 checks including a build-only
integration root with a forbidden target reference. Final project pins, native
fixtures, deterministic native builds and source freeze remain pending; no new
functional acceptance, installable release or live-readiness claim is made yet.


## Final source reviews and frozen aggregate — 2026-09-06

This section supersedes the earlier pending implementation/native/aggregate
statements. R independently accepted the native adapter and actual production
factory fixtures, the derivation checker, exact project closure and final aggregate
checker. A independently reviewed the wire validator and coverage matrix. All
component writes stopped before the candidate gate and initial source freeze.

The explicit unfrozen candidate gate passed. Its sanitized result SHA256 is
`69c292f1f61c3c4cb03acdb457eaa2544e9b7c457df002df1f6cd6cbba127d0d`.
The create-only freeze then created exactly 40 source records. R independently
verified the complete physical record set and each file hash. Source manifest:
`70136652a0ba6977061f25c4ab31f91c771b2831461b0b83c3788d66ccbdc1c8`.
Source inventory:
`d5dbc6ab497ab06d9fd9de3a06e17bde6f0a46207389cb6a4543c34ce3d646e1`.
No frozen contract, API, schema or predecessor byte changed.

The frozen aggregate passed by 16:44:10 UTC with `source_frozen:true`, the same
inventory, and sanitized result SHA256
`55d83b75f64d80855e81b91dc7c5d064bedce38048b23f01439371242707aa3e`.
Both aggregates verified all twelve predecessor inventories and original48:
`a0ca37bb2d0ad36fcb68eafe5163ac8174074b0e6f861c69c2ac357873f75f2a`.
Both used exactly two pinned compile references without executing target assemblies.

Each aggregate reports the following 244 checks across 12 suites. Counts are
reported suite checks/groups, not a claim of 244 distinct repository test cases.

| Suite | Passed checks |
| --- | ---: |
| Frozen item core | 10 |
| Frozen card core | 16 |
| Frozen completion actual native adapter | 87 |
| New event core | 12 |
| Actual child brokers | 6 |
| Wire service | 22 |
| Native parent | 10 |
| Actual production child factories | 3 |
| Provider-driven Python host | 37 |
| Aggregate project boundary | 22 |
| Actual C# / Python composition | 9 |
| Native source derivation | 10 |

The Release native library is 41,984 bytes at SHA256
`42861b780140f0644dc8c2bd2572522233a1544ca0c8b3c069885f8a6bcf123d`.
Each aggregate compiled it twice in separate clean locations; all four outputs
match. The library and target assemblies were never executed. The separate
1,243-test repository regression remains the result recorded above.

The corrected, separately reviewed metadata census succeeded once after its new
scope and 12 synthetic checks were accepted. Its
[result](PHASE_1_EVENT_COVERAGE_CENSUS_GENERIC_RESULT.md) records 68 concrete event
types, one custom node and two derived layouts. This supersedes the first census
attempt's absent denominator, without changing that historical failed result.
No event method bodies were read by the census. Runtime eligibility is unknown.
The independently reviewed [coverage matrix](PHASE_1_EVENT_COVERAGE_MATRIX.md)
contains all 68 types, separate predecessor live evidence and remaining branch gaps.
No event has evidence covering every branch.

Functional-only disposition: `release_ready:false`, `live_campaign_started:false`,
`target_assemblies_executed:false`. There is no new listener/bootstrap, installable
package, installation or campaign. The previous Smith/Cheese/Dense Vegetation
live evidence does not establish this new composition live. Next native work is
exact caller and effect proof for removal, transformation, event upgrades and
other addition/count variants. Custom layouts, embedded combat, repeated stable
option loops and optional/partial-domain selection remain explicit gaps.

R independently accepted the final frozen result and verified that candidate and
frozen result objects differ only in the intended `source_frozen` value. The
coordinator accepts this exact frozen functional component after that review.
There are no open blockers for this functional packet; broader coverage and
release/live gates remain the next work, not implied acceptance.
