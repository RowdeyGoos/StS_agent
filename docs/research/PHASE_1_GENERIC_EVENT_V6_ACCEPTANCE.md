# Generic event v6 acceptance ledger

2026-09-08. Functionally accepted and frozen under continued generic-handler development.
This ledger separates static evidence, focused fixtures, actual native integration,
source freeze and live claims. It does not grant a live campaign.

The full [G6 contract](../PHASE_1_GENERIC_EVENT_V6_CONTRACT.md), SHA256
`fa2a970c7a0a446d0993691bf9d1296f9ef846fd40befd0ed1019ab827147552`,
was independently accepted before native implementation. It adds one direct potion
or relic reward on an owned nonterminal ordinary reward screen; event identities
remain ownership/test data. Authoritative singleton admission occurs at ShowScreen
after awaited reward generation. Offer task completion and UI closure are separate
requirements. The [item discovery ledger](PHASE_1_GENERIC_ITEM_ACCEPTANCE.md) and
[native findings](PHASE_1_GENERIC_ITEM_NATIVE_EVIDENCE.md) own exact static evidence.

The new typed card/item wrapper reuses the actual frozen ItemV1Session, item codec
and strict parser. Existing card_selection_v1 and G5 card_transform_v1 engines,
codecs and parsers remain exact source links. Item continuation consumes one total
256-read budget and freshly revalidates claims/inventory after local resolution.
Item-only replay keys include outer lineage; naturally cached returned Tasks are
completion evidence under fresh observed invocation ownership. Separate cumulative
item/card counts preserve completion through Proceed and later failure.

## Ownership and interim evidence

A owns consumer core/wire/host and pure tests; B owns native discovery/adapters and
inert fixtures; R owns independent actual-native integrations and review; root owns
contract, explicit projects, checker, provenance, documentation and final freeze.
Build work is serialized in disposable /private/tmp snapshots. No game/Godot or
production native assembly executes. The game-owned Harmony may execute only
against inert stubs.

The first focused A snapshot `/private/tmp/g6-a-pure-b` passes1,718 item wrapper/core
assertions through the actual frozen session and118 retained wire cases, with zero
compiler warnings/errors. Host tests pass109 cases (95 retained and14 item).
These are interim counts; new adversarial and native tests remain in progress.
Independent wrapper review identified reentrant native cleanup recursively calling
Dispose; the fixed guard preserves ownership for an external retry and has an
explicit regression fixture. Root also requested exact last-action effect-family
validation so an item label cannot claim a previously completed card effect.

All25 frozen predecessors and the original bridge verify unchanged. The interim pending gates below were completed by the final acceptance recorded in the following sections. No listener/package/operator configuration
or live release is created by this functional component. All earlier campaigns
remain closed; no new game setup is needed.

## Final implementation and independent review

The exact consumer implementation passes the actual frozen item engine tests,
strict payload/lineage checks, shared budgets, and retained transformation engine.
Wire and host now validate the effect label against the latest completed parent
owner and its exact card/item family. The single parent controller uses all child
families without an event-name registry or an independent nested item controller.

Native review removed repeated clickability validation after native OnRelease:
strict control validation ends immediately before ForceClick, then the owned
GetReward invocation uses the retained receipt and domain. A compatible reward
button subclass is accepted while preserving exact captured reference and liveness.
Unsupported reward kinds are rejected before virtual IsPopulated is called; the
fixture proves zero custom-getter calls.

A stale inherited item scope now marks both its old binding and any different
current armed binding unsupported. The original native callback still runs; it
cannot be adopted as the new child's invocation. Tests complete item1, arm item2,
replay old request/collection execution contexts, and verify item2 stops while
item1's completion remains credited. Existing card hook rules are unchanged.

R independently accepted the final native and consumer sources. Its fresh
`/private/tmp/generic-event-v6-integration-a` builds passed without warnings/errors
and every reviewed source matched the tested snapshot. Tests cover both item kinds
under three event identities, identical later relic payloads, cached Task reuse
under fresh invocations, generation/collection/Offer/Chosen delays, disabled or
retired controls, compatible subtypes, hidden extra rewards, unsupported domains,
late claim/inventory mutation, lost replies, and malformed outer/inner correlation.
Mixed item→upgrade2→transform1→item→Proceed preserves separate cumulative counts.

A independently reviewed all nine project closures, the checker and exact frozen
reuse inventory. Root corrected the checker’s predecessor-adapter filename mapping
before any aggregate invocation. Source closure, Python 3.10 syntax and whitespace
checks passed. All implementation lanes closed before final provenance and freeze.

## Candidate, source freeze and frozen gate

Both complete gates pass 3,199 native assertions, 1,718 item assertions, 879 retained transformation assertions, 147 wire cases, 110 host tests and 161 integrations (62 pure, 99 actual-native).

- Candidate: `/private/tmp/generic-event-v6-root-candidate-a/result.json`, SHA256 `93b04b3df889d73afeda909a4fc5f72b4a0114e38c6f2d24537ca409238cd6cb`.
- Frozen: `/private/tmp/generic-event-v6-root-frozen-a/result.json`, SHA256 `9c94670eb668d0805cde3ba3251959d6d5cd978a67a8e7be90477280e9155dc0`.
- Source inventory: `042fd7a640980db8e971de022a5653f22942d9a86a288a2f83783c4581cf29bb`.
- Source manifest: `d6cc289c5722ff3f4c4501f728718a9562ebe60f2ef24eb79df0f185c0d064b8`.
- Frozen successor26: 55 inventoried files plus source_identity.json, 47 derivations and 21 exact reused C#/Python files.
- Production closure: 28 C# sources. Native assembly: 121,856 bytes, SHA256 `c35c6591d2c561682f660174e47766e137a6c4e677d77f0a57981c59be8fbf49`.

Both independent native builds within each aggregate match byte for byte, and the
artifact matches between candidate and frozen gates. All 25 predecessor successors
and the original bridge verify unchanged. No game, Godot or production native
assembly executed; pinned Harmony ran only against inert stubs.

This accepts generic singleton direct potion/relic children and all retained card
families. It does not certify multiple reward offers, optional/scrolling selectors,
variable upgrade/transform minima, custom screens, event combat, an installable
release, or live item/transform support. The relic claim remains reward-local; no
arbitrary relic inventory or hook-effect certification is invented.

No live campaign, installation, profile/save/Cloud work or remote Git action occurred.
All earlier campaigns remain closed. Next prepare a reviewed release composition
for a concrete live item test, or continue the remaining interaction families.
No game setup is needed before such a release is ready.
