# Generic event v3 acceptance ledger

2026-09-07. Authoritative integration worktree `/Users/rowdeygoos/.codex/worktrees/23cf/StS_agent`,
branch `codex/phase1-actor-ready-integration`, clean baseline `8e0877d`.
User request: continue with the next step of the generic event handler.

## Scope and ownership

The independently reviewed [v3 contract](../PHASE_1_GENERIC_EVENT_V3_CONTRACT.md)
extends shared discovery to reward offers followed by exact selected-original
additions1..8 with variable limits. Automatic mode submits at max without Confirm;
manual mode confirms at min..max. Upgrade-one and removal1..8 retain v2 behavior.
Event names supply representative/held-out tests, never semantic admission rows.
All sixteen predecessors and the original 48-file bridge remain unchanged.

Root owned contract/core/wire/checker/provenance/docs. Native lane owned native
(except derivation), native tests and retained evidence. Host/review lane owned
Python host/tests and independent review. Integration lane owned wire tests and
cross-language fixtures. One disposable SDK build lane was used at a time.

## Evidence and independent review

The [retained evidence audit](PHASE_1_GENERIC_EVENT_V3_REWARD_EVIDENCE.md) rehashed
shared request, result-card projection, exact creation overload, sorting and native
confirmation artifacts. No new target inspection was needed. The request provides
off-deck originals, not predispatch generation inputs or a global novelty claim.

The contract explicitly distinguishes selection from Add completion. Exact result
entries/effective-card references remain bound to the reserved owner/run and
baseline deck through creation and control. Native sorting may reorder slots.
The actual frozen card session proves selected-original add-only deck effects,
including monotonic partial progress, after exact request/selector sets and parent
success. A separate Add task, every add hook/animation and unrelated effects are
outside this bounded witness.

Independent final reviews found no actionable source defects in core, native,
wire, host, checker or integration. Upgrade/removal adapters were independently
compared to v2 and are exact renames. The reviewer verified all16 predecessor
identities,32 exact derivations,34 source files and current source/contract identity
against the successful candidate. Staged whitespace checks and Python3.10 parsing
of all6 Python files passed before source freeze.

## Candidate and frozen aggregate

Candidate `/private/tmp/generic-event-v3-root-candidate-a/result.json` passed;
SHA256 `c5a2cbece7180e1117025c6988b4454e66f429279daa52d1ab05688ef7c6ba12`.
The fresh frozen repeat passed at
`/private/tmp/generic-event-v3-root-frozen-a/result.json`. Its exact bytes are
[retained in the repository](PHASE_1_GENERIC_EVENT_V3_FROZEN_RESULT.json);
SHA256 `fa2097703f2f6d48e486eb7cc21e6d23e4c725ab6db5e286a58386b5595d2f69`.

Each aggregate passed all789 focused checks:

- 550 native assertions using real Harmony and production code against inert
  target stubs; all375 v2 assertions preserved.
- 95 wire checks; all73 v2 cases preserved.
- 71 Python host tests:33 original,19 removal and19 reward tests.
- 73 C#-to-Python integrations:50 shared-core and23 production-native cases;
  all45 v2 cases preserved, with16 shared-core and12 native reward additions.
- Reward evidence covers automatic/manual fixed and variable counts, max8/domain9,
  sorted/reverse offers and click order, delayed creation, partial additions and
  parent completion, mutated entries/effective cards/owners/results, forbidden
  controls, exact histories and lost final select/Confirm responses without retry.
- Mixed upgrade→remove→add episodes exercise the actual shared card/core/wire and
  Python path in the pure integration driver. The23 production-native integrations
  exercise families individually; a mixed-family native lifetime is not claimed.

Seven explicit project closures and exact derivation/reuse inputs passed. Both
aggregate runs compiled native code twice against the pinned target references
without executing target assemblies. All four native outputs matched exactly:
65,024 bytes, SHA256
`c934c369ebbab05f2e5a7f8f4dd1905be2f121174de11468dc79b4f85b8e2e9e`.
No target assemblies were copied into build output directories.

Harmony2.4.2 assembly SHA256 remains
`a849b726e1f9248d71aabbed8114deaf79beb7acc25e8344ff92a27ad8ac87ab`.
The checker validates the exact dependency package and pinned sts2/GodotSharp
reference identities. SDK9.0.303 and disposable offline build environments were
used. No simulator/training source changed; their unrelated full regression suite
was not rerun for this increment.

## Frozen identity and disposition

The34-file component identity is frozen:

- Manifest SHA256: `2a025c42f235985e7f22168e2e88f284aaec33876d91d098ebb8e24301f68025`.
- Source inventory SHA256: `5a2a75b7967f56b19d95b574599206c24ec74e4715de7eac380558d0687cd671`.
- Contract SHA256: `e7b940653dcfb48383ea2f08c312b97f48a24e323fdc540b45071dedc1ebd194`.

The frozen aggregate reverified every source hash and repeated all789 checks.
Source and contract bytes did not change after the passed candidate. Functional
acceptance is complete for upgrade-one, removal1..8 and reward additions1..8 in
this successor. There are17 frozen successors including v3.

Transformation, multi-upgrade, optional zero, scrolling, repeated-key choices,
item children, custom screens and event combat remain open. Fixtures confer no
named-event complete-branch or runtime eligibility claim. Packaging, installable
release composition and real EventSynchronizer context preservation remain
separate live gates. Lost ownership stops safely. No listener, installation,
live campaign, profile/save access, Cloud or remote Git operation occurred.
