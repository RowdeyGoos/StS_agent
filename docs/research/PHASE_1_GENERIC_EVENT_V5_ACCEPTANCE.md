# Generic event v5 acceptance ledger

2026-09-08. Functionally accepted, frozen and validated under user-directed
continued generic handler development. Baseline `771bec4` in the authoritative 23cf integration
worktree. All24 frozen predecessors and the original bridge remain unchanged.
All generic live campaigns remain closed; no new game setup is required.

## Accepted contract and ownership

R independently accepted the exact [G5 contract](../PHASE_1_GENERIC_EVENT_V5_CONTRACT.md)
before implementation:26002bytes, SHA256
`3c85df4d39bf8ffe6c64889e47dec2a73e0746848b8d66789351ab57974412ca`.
The document's prepared-stage header is preserved; this ledger records subsequent
implementation acceptance. This is scope acceptance, not completed validation.
The final [ordering evidence](PHASE_1_GENERIC_TRANSFORM_ORDERING_NATIVE_EVIDENCE.md)
was independently rehashed and accepted:9491bytes, SHA256
`44cfca64af50ea7cbe75d9052a6ce96e344d5def4b6ef6ace59eb8ed94da93f1`.
Its [ledger](PHASE_1_GENERIC_TRANSFORM_ORDERING_ACCEPTANCE.md) records the reviewed
single metadata invocation, successful capture and exact tool identities.

This increment supports fixed-count transform1..8 with explicit preview/Confirm,
a complete candidate domain larger than the selected count, and an exact native
effect journal. The new `card_transform_v1` child engine accepts native
remove-before-insert states and actual append order, including sequential batches
and substitution by native hooks. Earlier families retain the actual frozen
`card_selection_v1` session and wire parser. The outer protocol is
`generic_event_v5`, with exact `/probe/generic-event-v5/public/decision` and
`/probe/generic-event-v5/public/action` routes. Completion remains once per resolved
child and survives Proceed or later failure.

Root owns accepted contract, scaffolding, all build projects, checker, provenance,
identities and living docs. A owns core/facade/transform_core, wire, host and focused
consumer tests. B owns native adapters/hooks and native fixtures. R owns integration
sources/tests and independent review. SDK access remains serialized by root.
All implementation belongs to new generic_event_v5; predecessor artifacts are frozen.
Target references are compile-only. Pinned game-owned Harmony may execute against
inert target stubs only. This scope contains no package, listener, installation,
profile/save/Cloud work, target execution or live campaign.

## Validation gates

Required: focused transform-state, wire/host and actual native tests; production
compile against pinned references; actual-native through real core/wire/Python
integration; old-family and mixed-family regressions; independent implementation
and checker review; full candidate aggregate, source freeze, and full frozen
aggregate with reproducible native output and all24 predecessor identities.
No completed gate is claimed yet. Later sections will record exact results.

## Initial consumer checkpoint

A's pure core and wire Release builds passed. The new transform engine compiled
and passed806 assertions in `/private/tmp/g5-a-pure-tests`; Python host tests
passed95 cases (86 preserved plus9 transform tests). These include all fixed
counts, raw partial-deck rejection by the old positional core, observed append
prefixes, skipped sequential batches, successful command/result evidence,
malformed and regressing journals, bounded defensive copies, and256/257 pending
reads without progress resets. Wire's old test that classified transform as an
unsupported family is being updated alongside new tagged-version negatives.
This checkpoint is not the final consumer or aggregate acceptance.

Root and R independently reviewed the new core/journal and tagged wire/parser
paths without a current consumer blocker. Root's native review found the shared
temporary-reference set was still being compared with the old32-upgrade-clone
limit. Transform initial/final references require a separate shared64-reference
cap while preserving the existing upgrade-clone bound; B owns the correction
and mixed-family regression before native acceptance.

## Final independent review and focused handoffs

A's final `/private/tmp/g5-a-final-pure` passed879 transform assertions and118
wire cases with zero build warnings/errors; all95 host tests passed. Transform
codec/parser scope is child-only and earlier families retain the pinned frozen
parser. R independently reviewed native and consumer semantics. A independently
reviewed the root checker, eight explicit projects, dependency and provenance
construction; the exact external reuse set is eight C#/Python sources.

R's `/private/tmp/generic-event-v5-integration-b` passed122 scenarios:60 pure and62
through actual production native adapters/hooks with inert target stubs. Mixed
transform8 with substitution, another transform8, then upgrade2 completes three
children; a lost third Confirm or later rejection preserves two completions.

Final review found that insertion notifications could change final Owner/RunState
without changing reference/key/level. The native Matches guard now revalidates
exact player/run ownership for every current deck card after insertion and during
ongoing capture, covering both replacements and preserved originals. B's final
`/private/tmp/generic-v5-b-04` passed2345 native assertions, including eight new
notification/prefix owner/run cases, and compiled the production native adapter
against the pinned target APIs with zero warnings/errors. R independently accepted
the fix and added two combined owner/run callback regressions; the final aggregate
runs124 scenarios,64 actual-native. Earlier2049/2265 counts are interim only.

The final producer also keeps separate bounded parent-owned task/temporary/upgrade
reference sets:32 command tasks,64 temporary cards and32 actual upgrade clones.
Tests reject a reused command Task even when its mutable result list is changed
to resemble the next child. New hooks participate in exact foreign-patch checks,
rollback at all15 install positions and retained failed-cleanup recovery.

## Candidate, freeze and frozen validation

The first full candidate passed all final suites. Staged whitespace checking then
found one new blank line at the transform parser's EOF. Root removed only that
line and regenerated its derivation record. R independently verified that all
other source bytes stayed unchanged and accepted the revised inventory after
candidate B passed. No behavior change followed the final implementation review.

The exact candidate gate passed in `/private/tmp/generic-event-v5-root-candidate-b`,
result SHA256 `c2a6e4c97f44bc132dd2ad0e11fea1da503ce62ecef2b3ea5cc6dc05bb3f260b`.
The frozen gate passed in `/private/tmp/generic-event-v5-root-frozen-a`,
result SHA256 `c2a6e4c97f44bc132dd2ad0e11fea1da503ce62ecef2b3ea5cc6dc05bb3f260b`.
Both gates ran2345 native assertions,879 transform assertions,118 wire cases,
95 host tests and124 integrations (60 pure,64 actual-native). Both compile-only
native builds within each gate are byte-identical, and the native assembly matches
between candidate/frozen gates. Python3.10 AST checks and staged whitespace checks
also passed. All24 predecessors and the original bridge verified unchanged.

Frozen successor25 contains52 inventoried files plus source_identity.json:

- Inventory SHA256: `6eef170b1ca9c1986d0c97496493dfd4071491dd3607d91f21a0cce339d320a3`.
- Manifest SHA256: `9bc1ad289ecc9f25931e7666f428c6d6c1890e8665123ed7aeb83ef3de2475a5`.
- Native assembly:111104bytes, SHA256 `cecebfa29636792623cff90c8bbe5bb7a9ca8eb57baf6bbc72b9527802d00f71`.
- Explicit production closure:21 C# sources;41 exact derivations and8 frozen reused
  C#/Python entries. All source lanes closed before the manifest was written.

This accepts the fixed-count shared transformation family and retained prior
families, including mixed-family cumulative completion. It does not certify all
named event branches, optional/scrolling/variable-minimum transforms, variable
upgrade minima, generic item/custom/combat families, an installable release or
live transformation support. No game/Godot/production native assembly executed;
only the pinned game-owned Harmony ran against inert target stubs. No new live
campaign, installation, profile/save/Cloud operation or remote Git action occurred.

Next functional work is shared item-child discovery/admission, with remaining
selector variants kept explicit. Release composition and live validation are
separate gates; no game setup is needed during continued functional development.
